/**
 * Server-level MCP instructions — the orientation string returned to every agent
 * in the `initialize` response, before its first tool call. This is the single
 * highest-leverage piece of agent guidance: it's the only text every connecting
 * client is guaranteed to receive up front (per-tool descriptions are only seen
 * once a tool is inspected).
 *
 * Keep it WORKFLOW- and GOTCHA-focused, not an exhaustive tool list (tools carry
 * their own descriptions). Every concrete claim here is verified against the tool
 * schemas/handlers — when a tool, handle field, or param name changes, update this
 * string in the same commit (the content contract is asserted by
 * tests/unit/mcp-instructions.test.ts).
 */
import { STUDIO_NAME } from '../constants.js';

export const MCP_SERVER_INSTRUCTIONS = `${STUDIO_NAME} is an SEO / web-analytics agency platform. Most tools operate on ONE client workspace and need a workspace id — call \`list_workspaces\` first to get ids, and \`get_workspace_overview\` for a snapshot of pending work. Workspace creation/listing and the studio template-library tools are explicit global tools and require the master key. \`get_pending_work\` may omit \`workspaceId\` for a cross-workspace summary with the master key; a per-workspace key must always supply its own workspace id. NOTE the casing split: most tools name the parameter \`workspace_id\` (snake_case), but several tools (workspace overview/intelligence, insights, content reads, client signals, and legacy brand identity) use \`workspaceId\` (camelCase). Don't assume — match each tool's own schema.

CONTENT AUTHORING IS A HANDLE PIPELINE. \`prepare_brief_context\` returns a \`brief_request_handle\` plus the brief schema — YOU generate the brief locally, then \`save_brief\` (returns a \`brief_handle\`). Repeat with \`prepare_post_context\` → \`save_post\` (returns a \`post_handle\`). Finally \`send_to_client\` turns a saved brief/post into a client-facing request AND emails the client. Handles are single-use, expire ~15 minutes after creation, and are scoped to one workspace + kind: if a handle errors as not-found or expired, re-run the tool that produced it — never retry the consumer with a stale handle.

PREFER SERVER-SIDE GROUNDED GENERATION FOR HEAVY WRITING. Instead of authoring locally, \`start_brief_generation\` and \`start_post_generation\` ([Paid API]) run the full research-backed brief/post generation on the server as a background job (GSC/GA4 enrichment, SERP + competitor scraping, section-by-section drafting, unification, SEO meta) and persist the result. They return a \`job_id\` — poll \`get_job_status\`, then read the result with \`get_brief\`/\`get_post\`. Use \`start_post_generation\` with a saved \`brief_id\`; use the local handle pipeline only when you need to hand-author or heavily steer the draft.

CONTENT MATRIX GENERATION IS PREVIEWED, BOUNDED, AND REVIEW-GATED. Template and matrix variables use a single brace pair, for example \`/{service}-{city}\`; never use \`{{double_braces}}\`. When the page structure is already known, use \`create_content_template\`, verify it with \`get_content_template\`, then call \`create_content_matrix\` with that template ID and explicit Cartesian dimensions; Page Strategy is not required. A template section with \`optional: true\` is evidence-driven: its exact \`matrix-cell:<cell_id>:section:<section_id>\` requirement includes it when verified and otherwise reports an \`optional_omit\` omission without blocking generation; every template must retain at least one non-optional section. Proven generation-ready structures can be reused across clients through the master-key studio library: only after a human operator explicitly requests the exact promotion, \`promote_template_to_library\` snapshots that revision under a vertical; \`list_library_templates\`/\`get_library_template\` read the immutable asset, and \`instantiate_library_template\` creates a normal workspace-owned copy with fresh section IDs. Copies never inherit or synchronize later library/client edits. For a Page Strategy collection entry, \`get_pseo_matrix_plan\` plus \`create_content_matrix_from_pseo_plan\` remains the blueprint-linked convenience route. Creation never previews or starts generation. Use \`list_content_matrices\`, then \`get_content_matrix\` for bounded matrix/cell pages. Use \`update_content_matrix_cell\` with the cell's current \`expected_cell_revision\` for an intentional per-cell keyword, URL, variable, or schema override; URL overrides keep strict path and workspace collision checks, advance the cell revision, and invalidate prior previews. Pass each selected cell's exact \`expected_source_revision\` to \`resolve_content_matrix_cells\`; a source edit conflicts instead of mixing snapshots. Resolution returns structural targets, blockers, or an exact legacy-template upgrade proposal—it does not call AI or create a generation run. Apply or reject that exact proposal with \`accept_content_template_generation_upgrade\`; acceptance is version-conditional. Then use \`preview_content_matrix_generation\` to freeze finalized voice, approved identity, evidence requirements, optional omissions, artifact revisions, fingerprint, and paid-work estimate without calling AI. Resolve a stable factual gap with \`resolve_content_matrix_evidence\`; that mutation advances only the cell revision, so re-read and re-preview with the new \`expected_source_revision\`. Start paid work with \`start_content_matrix_generation\`, passing every exact preview fingerprint plus caller-accepted hard limits and an idempotency key. Poll \`get_job_status\`, then page durable per-cell outcomes with \`get_content_matrix_generation\`; use \`retry_content_matrix_generation\` only for explicitly selected failed/needs-attention checkpoints with current run/item/source/artifact revisions. Generation stops at human review. MCP can observe approval evidence but cannot approve or publish a page.

BRAND INTAKE IS IMMUTABLE AND EVIDENCE-ADDRESSED. Use \`get_brand_intake\` to read the current or named intake revision plus field-level evidence availability. To correct or evidence-resolve an exact current field, pass that exact \`intake_revision_id\`, \`expected_revision\`, stable \`requirement_id\`, matching \`field_path\`, typed value, factual \`source_ref\`, and a caller-stable \`idempotency_key\` to \`resolve_brand_intake_evidence\`. A resolution creates or reuses a superseding revision; it never mutates the source revision in place. Re-read after a conflict. Placeholder text or deleting text never counts as evidence.

BRAND VOICE FINALIZATION REQUIRES A HUMAN OPERATOR. Call \`get_pending_approvals\` for the complete Brand & AI human-review queue, or \`get_brand_voice\` to inspect exact missing prerequisites, pending chat proposals, and one bounded page of eligible authentic anchor samples. Use \`add_brand_voice_samples\` when authentic examples arrive as a set; they remain proposals until approved in the platform. Generated calibration or approved-output samples are forbidden as anchors. While \`eligible_anchors.has_more\` is true, call \`get_brand_voice\` again with \`anchor_cursor=eligible_anchors.next_cursor\`; if a cursor conflicts, restart from the first page because the profile or intake revision changed. An authenticated human operator must create a short-lived authorization at the HTTP boundary, bound to the exact profile revision, voice fields, anchors, ratings, and idempotency key. Then call \`finalize_brand_voice\` with its one-time \`authorization_token\`. The MCP key is only the executor and cannot supply or impersonate the finalizing operator; its internal execution identity is never returned in either tool response. If finalization conflicts, re-fetch all \`get_brand_voice\` pages and ask the operator for a new authorization; never retry a stale authorization.

BRAND DELIVERABLE GENERATION IS DURABLE, PAID, AND REVIEW-GATED. Call \`start_brand_deliverable_generation\` with one exact immutable intake revision/fingerprint, an explicit bounded budget, and a caller-stable idempotency key. Every durable target requires the exact finalized voice version/fingerprint. The \`full_brand_system\` preset is the sole bootstrap exception: its first job creates only a provisional \`voice_foundation\` and stops at \`awaiting_voice_finalization\`; it creates no dependent deliverables until a human explicitly finalizes voice and you call \`resume_brand_deliverable_generation\` with the exact run revision and finalized voice authority. Poll \`get_job_status\`, then page durable run items through \`get_brand_generation\`. Use \`start_brand_deliverable_revision\` only with exact run/item/deliverable versions and explicit review direction. When the selected items are ready and have no unresolved client-input requirements, call the existing \`send_to_client\` with exactly one \`brand_generation\` target containing \`run_id\`, \`expected_run_revision\`, and \`review_kind\`; voice foundation and durable brand suite are separate grouped Inbox reviews. The generation command stops at human review and never auto-approves/sends/publishes; naming/taglines are creative proposals—not clearance claims. Only an authenticated human can approve or request changes item by item in the client portal. A conflict means re-read the run or deliverable before retrying; never force a newer human edit aside.

BRAND-TO-CONTENT ONBOARDING COORDINATES THOSE EXISTING GATES. Call \`start_brand_content_onboarding\` with one exact intake revision, a non-empty exact matrix-cell selection, an explicit brand budget, and a stable idempotency key. Brand-only work belongs to the standalone brand-generation tools. Onboarding starts only the existing \`full_brand_system\` child and returns a durable run; poll any returned \`paid_job_id\` with \`get_job_status\`. Use \`get_brand_content_onboarding\` to read the current gate and \`resume_brand_content_onboarding\` with its exact revision, status, and named durable gate evidence; each resume evaluates at most one gate and may return another \`paid_job_id\`. MCP cannot provide the human content authorization, send brand reviews, finalize voice without the existing operator authorization, approve pages, or publish. Human review and content authorization remain at their existing authenticated HTTP/client boundaries. \`ready_to_publish\` means every selected page has explicit approval and passed a fresh publish preflight—it does not publish anything.

EDITING EXISTING ITEMS USES OPTIMISTIC CONCURRENCY. For briefs/posts call \`get_brief\`/\`get_post\` for the current \`revision\` token and pass it back as \`expected_revision\`; for brand deliverables call \`get_brand_identity\` (with \`includeDeliverables: true\`) for the current \`version\` and pass it as \`expectedVersion\` to \`update_brand_deliverable\`. On a conflict error, re-fetch and retry — do not force the write.

KEYWORD RESEARCH. \`research_keywords\` ([Paid API] — one paid call per term) returns a \`research_handle\` you pass to \`add_keyword_to_strategy\`.

PAID APIS COST REAL MONEY — use deliberately: \`research_keywords\`, \`start_keyword_strategy_generation\`, \`start_local_seo_refresh\`, \`start_brief_generation\`, \`start_post_generation\`, \`start_content_matrix_generation\`, \`retry_content_matrix_generation\`, \`start_brand_deliverable_generation\`, \`resume_brand_deliverable_generation\`, \`start_brand_deliverable_revision\`, \`start_brand_content_onboarding\`, conditionally \`resume_brand_content_onboarding\`, and \`get_workspace_intelligence\` when \`enrich_with_backlinks\` or \`resolve_entity_references\` is set. Long-running commands return a \`job_id\` or onboarding \`paid_job_id\`; poll it with \`get_job_status\` rather than blocking.

DESTRUCTIVE / IRREVERSIBLE — confirm intent before calling: \`delete_workspace\`, \`delete_brief\`, \`delete_post\`, \`replace_keyword_strategy\`, \`revert_post_version\`.

WORKSPACE WRITES ARE ATTRIBUTED INTERNALLY. Activity records capture the MCP request, tool, and authenticated key identity for operator audit. Per-key ids and labels are internal-only and are removed from client-facing activity and live broadcasts.

ERROR CONTRACT. Every tool uses the \`json_v1\` stable JSON envelope in the text payload with \`code\`, \`message\`, \`retryable\`, and optional safe \`details\`; raw arguments, prompts, secrets, evidence, exceptions, and stacks are never returned.`;
