// @ds-rebuilt
// Re-export the canonical error extractor so mutation feedback keeps the same API-error coverage.
export { extractErrorMessage as mutationErrorMessage } from '../../lib/extractErrorMessage';
