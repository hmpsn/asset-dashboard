# Rebuilt Surface Inventory Grading

Source of truth for mounted rebuilt surfaces: `src/components/layout/rebuiltSurfaces.ts`.

This is an initial behavior-first triage, not a final design review. A surface can look polished and still be a behavior mismatch if it uses the wrong interaction model.

Accepted directions and their circle-back triggers for the behavior mismatch and capability risk buckets are recorded in `docs/ui-rebuild/parity/owner-decision-packet.md`.

Route coverage note: this inventory grades mounted rebuilt route families, not every admin `Page` value. `docs/ui-rebuild/parity/coverage-audit.md` is the current route/nav census. It shows that `page-intelligence` and `content-perf` are standalone nav entries that are not in `REBUILT_SURFACES`; `seo-briefs`, `content`, `calendar`, and `subscriptions` are folded or redirect-only; `workspace-settings` remains a per-workspace settings receiver, not a main sidebar item. `competitors` is still globally `NON_REGISTRY_PAGES`, but the rebuilt sidebar now surfaces it in the prototype `Strategy & Content` zone because the rebuilt shell is flag-gated.

## Aligned Enough

| Surface | Reason | Next check |
|---|---|---|
| `home` / Cockpit | Current calibration surface. Verdict-first structure is the right rubric, and this branch fixes the mobile shell rail issue plus the narrow client-context header squeeze exposed by smoke. Activity and work-order overlays are now browser-captured, and the Cockpit rail/stream primitives now use styleguide typography roles instead of raw pixel text shims. | Keep as the rubric while grading the remaining registry surfaces. |
| `brand` / Brand & AI | Modal-first correction is implemented: the overview uses the prototype's grouped context inventory, removes the legacy top tab strip, scopes Brand identity generators to Voice & Messaging, opens non-overview workflows in modals while preserving `?tab=` aliases, and the Brand identity, Discovery, Brandscript, Trust evidence, Business facts, Context editors, Voice calibration, and Strategy intelligence modals now expose prototype workflow framing before carried editor panels. | Continue modal-interior polish only where it wraps legacy panels without rewriting them; defer legacy panel rewrites to an owner-approved slice. |
| `seo-schema` / Schema | Contract pass shows the rebuilt surface already preserves the prototype's core five-step generator workflow, guide mode, validation gate, page detail review, publish/send actions, and history. Safe cleanup removed internal rebuild language, fixed the admin recommendations read path, aligned the Workflow Guide primary phases to Scan / Review / Edit / Publish / Validate, mapped guide/drawer workflow explanations to `.t-body` and guide action rows to `.t-ui`, and browser smoke covers desktop, mobile, guide deep link, and detail drawer states. | Accepted default: keep the current Drawer detail. |
| `links` / Links | Contract pass shows the rebuilt surface already matches the prototype's four-mode link workshop: Redirects, Internal Links, Dead Links, and Architecture. Safe cleanup removed implementation/deferred language, keeps the default route clean, preserves `?tab=` and the legacy `dead` alias, adds the prototype-style repair-to-measured-outcome footer, maps workshop instruction copy to `.t-body`, and tests prove the critical drawer/deep-link behavior. | Accepted default: keep HTML copy/client-send until a real Insert write target exists. |
| `performance` / Performance | Contract pass shows the rebuilt surface already matches the prototype's two-mode speed workflow: Page Weight and Page Speed. Safe cleanup removed implementation/deferred language from PageSpeed detail, keeps the default route clean, preserves `?tab=weight` and `?tab=speed`, exposes the Asset Manager repair handoff in PageSpeed, maps detect/repair guidance to `.t-body`, and tests prove critical drawer/deep-link behavior. | Accepted default: keep Drawer detail and the current strategy/snapshot workflow until the Asset Manager handoff is proven. |
| `competitors` / Competitors | Contract pass shows the rebuilt surface already matches the prototype's single competitive-intelligence stack: saved competitor set, alerts, share of voice, head-to-head, keyword gaps, and backlink profile. Safe cleanup restored the prototype `Keyword gaps` label, removed provider/cache phrasing from loaded states, folded the saved competitor set into a prototype-style `Competitive intelligence` header summary, replaced the generic alert table with a prototype-style alert feed, kept Workspace Settings as the set-edit home, and preserves the production-only detail Drawer, Hub deep link, Create brief, and flag-gated Send to client affordances. | Keep the current Drawer/detail unless Joshua wants exact prototype card composition as a visual polish pass. |
| `seo-keywords` / Keyword Hub | Contract pass shows the rebuilt pilot already matches the prototype's five-lens keyword workflow: Rankings, Opportunities, Pages, Clusters, Lifecycle, plus drawer detail and client feedback. Safe cleanup removed old command-center/No CPC empty copy, fixed the mobile header squeeze found in smoke, mapped client feedback, measurement context, SERP proof, and local evidence to styleguide typography roles, and preserved production-only lifecycle safety, protected bulk actions, hard delete eligibility, local evidence, and live invalidation. | Keep inline row sparklines and avg-position/delta summary KPIs deferred until server-owned read models exist. |

## Behavior Mismatch

| Surface | Reason | Required correction |
|---|---|---|
| `content-pipeline` / Content Pipeline | Board-first parity slice is independently accepted: bare/default route renders Intake plus Queued/Brief/Draft/Review columns; zero-count Brief/Draft launchers remain usable; visible modes are Board, Calendar, Published, Content Health, and Matrix; legacy workspaces, aliases, post state, and fix context remain exact once. Browser smoke is clean. | Keep the aggregate Board as the accepted `ODP-002 C` first phase. Later slices add item-backed Brief/Draft workspaces and the prototype capacity drawer. |
| `seo-editor` / SEO Editor | Independently accepted first phase: Static, CMS, and Manual rows render as explicit source groups while retaining the existing one selected-row action region, Detail Drawer, URL state, and write semantics. The rowful 503-target browser fixture is clean. | `ODP-003 C` first phase is complete. Inline editing and keyboard review queue remain deferred until every write/approval path is reviewed. |
| `seo-audit` / Site Audit | Independently accepted slice: only Site Audit and History remain visible peer modes. AI Search Ready, Content Health, and Audit Guide are exact-once disclosures inside the audit flow, including during audit progress; existing `?sub=` values open the requested evidence without replacing the audit body. Browser smoke is clean. | Accepted `ODP-004 A` slice is aligned. Revisit only if operator review finds the demoted disclosures too subtle. |
| `analytics-hub` / Search & Traffic | Contract pass shows the rebuilt surface preserves the core reporting capabilities, but the prototype's per-client default is Search performance / Site traffic / Annotations while the current rebuilt route still defaults to a peer Overview lens. Safe pre-work removes implementation row labels, opens conversion evidence by default, retints users/sessions away from teal action color, wraps the loaded header, maps report context/explanatory copy to styleguide typography roles, and tests prove flag transition, deep links, and Breakdowns Drawer behavior. | `ODP-005 A + C`: default to Search performance, preserve `?lens=overview`, and rehome useful Overview content. |
| `media` / Assets | Contract pass shows the prototype is a single Asset Manager workshop where filters and bulk repair actions are the navigation surface. The rebuilt surface preserves browse, audit, upload, CMS, organize, compression, alt, rename, delete, and drawer workflows, but exposes Browse / Audit / Upload as peer lenses. Safe URL cleanup, header/source-fix copy, audit fallback language cleanup, responsive-control cleanup, audit repair handoff context, Browse source-fix/proof framing, focused tests, and current-state smoke are complete. | `ODP-006 C`: phase into one workshop with Browse default, Upload in the toolbar, and Audit as repair results. |

## Capability Risk

| Surface | Risk | Required contract focus |
|---|---|---|
| `rewrite` / Page Rewriter | Contract pass shows the core two-pane rewrite workspace is aligned enough after safe cleanup, export-only footer polish, and typography role alignment, but legacy/prototype Focus mode is missing from the rebuilt shell and the prototype's Save draft / Publish rewrite spine does not exist as a backend capability today. Follow-up smoke found and fixed a loaded/pending-state issue where a loaded page could keep showing skeletons instead of the document footer, and the current workspace now keeps assistant/document/export copy on `.t-body` / `.t-ui` roles. | `ODP-007 A`: add one shell focus bridge and retain export-only v1 until a backend write-spine project is approved. |
| `local-seo` / Local Presence | Safe correction now matches the prototype's two-mode interaction model (`Rank & profile`, `Reviews & replies`) and setup drawer workflow while preserving `?lens=`/legacy `?tab=` receivers. The current real-data rank/profile workspace also now maps setup/GBP/trend explanations to `.t-body`, competitor/market labels to `.t-ui`, and snapshot counts to blue data color. Full prototype parity still depends on unbuilt data sources: 49-point geo-grid nodes, GBP Performance metrics, fuller profile-health fields, View-on-Google URLs, and reopen transitions. | `ODP-008 A`: keep real-data/manual-refresh v1 and backlog geo-grid/GBP Performance as backend slices. |
| Global ops surfaces (`settings`, `workspace-settings`, `roadmap`, `revenue`, `ai-usage`, `features`, `prospect`, `outcomes-overview`, `outcomes`, `diagnostics`, `requests`) | Contract pass is drafted and confirms the current safest shape is an additive route-family shell: route receivers are preserved and dense live operator machinery stays mounted exactly once, but several prototype moves require owner/backend decisions before exact IA collapse. Safe cleanup removed visible process terms, and smoke found/fixed the workspace-less global-route mount bug for `/settings`, `/roadmap`, `/revenue`, and `/outcomes-overview`. | `ODP-009 A`: retain the additive shell and follow accepted `GO-001` through `GO-008` deferrals. |

## Needs Contract Pass

All currently mounted rebuilt admin route families have an initial behavior-first contract. Continue adding packets when new rebuilt routes are mounted in `REBUILT_SURFACES`.

## Visual-Only Mismatch

| Surface | Reason | Required correction |
|---|---|---|
| `seo-strategy` / Insights Engine | Calibration accepted: one 1180px strategy spine, responsive anchored/open `?lens=` receivers, exact-once overlays/actions, read-only detail for every move type, truthful four-group stance geometry, staged-and-sendable projections, query-authoritative keeper state, honest attention/error states, locally light horizontal client preview, and preserved legacy redirects. Joshua and seven fresh Sol review rounds found composition, truthfulness, responsiveness, and capability-placement gaps; every recorded finding is corrected, and the final independent Sol Ultra review returned `PASS`. | Use as the Wave 2 worker rubric. Keep owner visual re-review logged as a circle-back without inferring sign-off. |
