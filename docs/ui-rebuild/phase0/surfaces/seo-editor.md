# Phase 0 Additive-Parity Ledger — SEO Editor (zone: Optimization)

Audited at HEAD of `ui-rebuild-phase-0` (== post-Reconcile `origin/staging`). Read-only audit; every claim carries `file:line` evidence.

- **Page id:** `seo-editor` (`src/routes.ts:5`), nav entry `src/lib/navRegistry.tsx:145` (group `optimization`, `needsSite: true`, "Edit titles, descriptions, and meta tags").
- **Mount:** `SeoEditorWrapper` → unified three-source editor (`src/components/SeoEditorWrapper.tsx:23-27`), composing `SeoEditor` (static pages), `CmsEditor` (CMS items), a Manual-URL section, and a unified `PendingApprovals` panel.
- **Governing contract:** `docs/rules/seo-editor-write-targets.md` — every write flows through a resolved target type (`static-page` | `cms-item` | `manual`); manual rows are never writable.
- **Prototype view read:** `hmpsn studio Design System/mockup/editor.js` (all 1738 lines).

---

## 1. Capability table

Status legend: **preserved** (obvious home, same/better) · **improved** (prototype upgrades it) · **new_proposed** (prototype-only, needs sign-off) · **at_risk** (exists at HEAD, no visible home in prototype).

### A. Shell, sources, filtering

| # | Capability | Evidence (HEAD) | Status | Home in new IA | Notes |
|---|---|---|---|---|---|
| A1 | Unified source model: Static / CMS / Manual write targets with per-type counts + source filter buttons | `src/components/SeoEditorWrapper.tsx:43-89`; resolver `src/components/editor/seoWriteTargetResolver.ts:39-147`; shared type `shared/types/seo-editor-write-target.ts` (imported at Wrapper:12) | improved | SEO Editor — scope segments All/Static/CMS/Manual | Prototype keeps the exact source model incl. counts (`editor.js:1337-1341`) and grouped sections (`editor.js:1216-1221`). |
| A2 | Per-target capability matrix (canSave/canPublish/canSendToClient/canAnalyze/canBulkRewrite), manual = all false | `seoWriteTargetResolver.ts:57-63,103-109,130-136` | preserved | Row-level enable/disable | Prototype renders manual rows read-only, unselectable, no actions (`editor.js:1153,1157-1158`). |
| A3 | Manual CMS URLs section: sitemap URLs unmatchable to collection items, shown for awareness with reason text; empty state when none | `SeoEditorWrapper.tsx:165-195`; reason at `seoWriteTargetResolver.ts:137` | improved | Manual section | Prototype adds richer per-URL remediation guidance ("Add a redirect / recreate as collection item / edit at source", `editor.js:927-945`) and an "Add redirect" CTA (new — see N9). |
| A4 | CMS collection filter dropdown (per-collection item counts) | `SeoEditorWrapper.tsx:90-104`; options built at `seoWriteTargetResolver.ts:70-75` | preserved | Collections dropdown (`editor.js:1314-1317`) | |
| A5 | Unified search across static pages + CMS items (+ manual titles/paths) | `SeoEditorWrapper.tsx:106-115,53-57`; static filter `src/components/SeoEditor.tsx:162-165` | improved | Header search (`editor.js:1332`) | Prototype search also matches target keyword + collection name (`editor.js:712-715`). |
| A6 | Loading states ("Loading page metadata…", "Loading CMS collections…") | `SeoEditor.tsx:235-242`; `CmsEditor.tsx:147-154` | preserved | Standard state matrix | Prototype is static; Build Conventions state matrix owes loading/empty/error/locked per surface. |
| A7 | Empty states: no CMS collections; no items matching collection filter; no manual URLs | `CmsEditor.tsx:156-160,194-196`; `SeoEditorWrapper.tsx:188-195` | preserved | Standard state matrix | Prototype has "No targets match this filter" (`editor.js:806,1206`). |
| A8 | Data reads: `GET /api/webflow/all-pages/:siteId` (static+CMS-discovered pages) and `GET /api/webflow/cms-seo/:siteId` + approval batches | `src/hooks/admin/useSeoEditor.ts:29`; `src/hooks/admin/useCmsEditor.ts:69-70` | preserved | Same APIs | Props-are-the-contract rule applies. |
| A9 | Refresh pages from Webflow (invalidate + refetch) | `SeoEditor.tsx:259`; button `src/components/editor/SeoEditorHeaderActions.tsx:61-68` | preserved | Topbar "Re-sync targets" (`editor.js:1536`) | |

### B. Per-page editing (static)

| # | Capability | Evidence (HEAD) | Status | Home in new IA | Notes |
|---|---|---|---|---|---|
| B1 | Expandable per-page row: title, slug/path, missing-title/meta badges, unsaved badge, tracking-status border + badge | `src/components/editor/PageEditRow.tsx:74-152` | improved | Worksheet row + drill drawer | Prototype renders as spreadsheet row with the same flags (`editor.js:733-737,1177-1188`). |
| B2 | Edit SEO Title + Meta Description with color-coded CharacterCounter (60/160) | `PageEditRow.tsx:273-369` | preserved | Sheet cells + drawer fields with live counters (`editor.js:895-902,1159-1176`) | |
| B3 | Save to Webflow — `PUT /api/webflow/pages/:pageId/seo`, writes `seo` **and mirrored `openGraph` title/description** | `src/components/editor/useSeoEditorPageWorkflow.ts:96-140`; server `server/routes/webflow.ts:192` | at_risk | Drawer "Publish" (`editor.js:1044-1046`) | The OG-mirroring write is invisible in the prototype (it edits title/meta only; FB preview implies OG). Must be preserved in the save payload. |
| B4 | Save Draft locally (localStorage, per workspace+page) | `useSeoEditorPageWorkflow.ts:48-94` | improved | Prototype "Save draft" is a first-class lifecycle status (`editor.js:1685,1639`) | Status-based draft supersedes localStorage draft **if** draft persistence survives reloads — see Q6. |
| B5 | Per-field inline error states (network/permission/validation) with auto-dismiss | `useSeoEditorPageWorkflow.ts:37,111-127,141-156`; render `PageEditRow.tsx:185-194` | at_risk | — | Prototype has no error rendering (static mock). Mutation-feedback contract must carry these. |
| B6 | AI rewrite per field ('title' | 'description' | 'both') → `POST /api/webflow/seo-rewrite`; returns text, 3 variations, or paired title+desc sets | `useSeoEditorPageWorkflow.ts:166-212`; server `server/routes/webflow-seo-rewrite.ts:32` | preserved | AI buttons per field + "AI generate set" (`editor.js:891,895,900,1664-1683`) | Purple = admin-AI only, honored by prototype. |
| B7 | AI variation picker — 3 options per field with char counters; paired title+desc picker for 'both' | `PageEditRow.tsx:235-271,301-319,350-368` | at_risk | — | Prototype AI writes a single value directly into the field (`editor.js:1667-1668,1678`); the choose-1-of-3 (and paired-set) capability has no visible home. |
| B8 | Google SERP preview + Facebook social preview per page (toggleable) | `PageEditRow.tsx:424-466` (SerpPreview/SocialPreview) | improved | Always-live previews in drawer + docked SERP strip (`editor.js:915-921,1230-1292`) | Prototype previews update live on keystroke (`editor.js:1054-1066`). |
| B9 | Analyze Page → keyword analysis + auto-persist to `keywordStrategy.pageMap`; "Analysis on file" indicator; Re-analyze | `useSeoEditorPageWorkflow.ts:214-249` (`keywords.analyze` + `keywords.persistAnalysis`); UI `PageEditRow.tsx:196-219` | at_risk | Research mode "Page intelligence" card (`editor.js:967-1004`) | Prototype *shows* gaps/recommendations but has no explicit Analyze/persist action or "analysis on file" state; the persist-to-strategy contract (feeds AI rewrite context, `FEATURE_AUDIT.md:4471`) must not be dropped. See Q2. |
| B10 | Target keywords (primary + up to 3 secondary) shown on collapsed row, joined from strategy pageMap via `usePageJoin` | `SeoEditor.tsx:54-72`; render `PageEditRow.tsx:102-115` | improved | Row keyword cell + in-title/in-meta match dots (`editor.js:1137-1146`) | Prototype adds keyword-match indicators + rank. |
| B11 | Recommendation flags per page from recommendations engine (metadata recs: title, traffic-at-risk, insight, estimated gain, priority now/soon/later) — amber border + banner | `SeoEditor.tsx:45,152-160`; `PageEditRow.tsx:70-72,131-135,156-184` (`useRecommendations`) | at_risk | Research-mode recommendations list (`editor.js:972-996`) is seed-data heuristics, not the recommendations engine | Wiring `useRecommendations` → row flags + sort boost has no confirmed home. See Q3. |
| B12 | Recommendation-weighted sort: flagged pages first (`filterAndSortSeoPages`) | `SeoEditor.tsx:162-165`; `src/components/editor/seoEditorDerived.ts` (filter/sort helpers) | at_risk | Prototype sorts by on-page score ascending (`editor.js:808,1217`) | Different sort key; rec-flag priority ordering is lost unless the score folds recs in. |
| B13 | Per-page "Send to client" (single-page approval batch `SEO Review — <page>`) | `src/components/editor/useSeoEditorApprovalWorkflow.ts:57-94`; button `PageEditRow.tsx:373-388` | preserved | Drawer/row "Send" (`editor.js:1244,1686`) | |
| B14 | fixContext deep-link: cross-surface "Fix in SEO Editor" (Site Audit, Page Intelligence, Cannibalization triage, dead links) auto-expands + scrolls to the target page by id/slug; re-fires per new target | `src/components/editor/useSeoEditorSessionState.ts:29-86`; contract `src/types/fix-context.ts:1-40`; anchor `PageEditRow.tsx:75` | at_risk | — | Prototype has internal `jumpFix` (`editor.js:1596-1600`) but no inbound cross-surface focus contract. Every "Fix in editor" CTA platform-wide depends on this. |
| B15 | Session persistence: unsaved edits, variations, expanded rows cached (localStorage + RQ) so they survive Pages↔CMS and admin tab switches; cache-restore skip on remount | `useSeoEditorSessionState.ts:37-66`; `src/components/editor/seoEditorPersistence.ts` (219 lines) | at_risk | — | Prototype state is in-memory only. Losing this regresses `FEATURE_AUDIT.md:4636` behavior. |

### C. Bulk operations (static)

| # | Capability | Evidence (HEAD) | Status | Home in new IA | Notes |
|---|---|---|---|---|---|
| C1 | Missing-titles / missing-descriptions counts + one-click "AI Fix Titles/Descriptions (N)" → background `BULK_SEO_FIX` job (`POST /api/webflow/seo-bulk-fix/:siteId`) | `src/components/editor/SeoEditorHeaderActions.tsx:46-87`; `src/components/editor/useSeoEditorBulkWorkflow.ts:208-247`; server `server/routes/webflow-seo-apply.ts:29` | preserved | Fix banner "Fix N titles / N desc" (`editor.js:828-841`) + "Missing tags" quick chip | |
| C2 | Multi-select pages (checkbox per row, select-all over filtered set) | `useSeoEditorApprovalWorkflow.ts:46,53-55`; `src/components/editor/BulkOperations.tsx:45-55` | improved | Row checkboxes + per-source select-all + sticky bulk bar (`editor.js:815-826,1196-1201`) | |
| C3 | Bulk "Send to Client" of selected pages with changes (approval batch `SEO Changes — <date>`; writable-only filter; "no changes" toast) | `useSeoEditorApprovalWorkflow.ts:96-124`; `ApprovalPanel.tsx:15-31` | preserved | Bulk bar "Send to client" (`editor.js:821`) | |
| C4 | Pattern Apply: append/prepend text to selected pages' title/description, diff preview, apply (`POST /api/webflow/seo-pattern-apply/:siteId`), 60-char truncation note | `useSeoEditorBulkWorkflow.ts:249-282`; UI `BulkOperations.tsx:99-160,162-209`; server `webflow-seo-apply.ts:40` | at_risk | — | Absent from the prototype entirely. |
| C5 | Bulk AI Rewrite (title/description/both) of selected pages → background `SEO_BULK_REWRITE` job (`POST /api/seo/:workspaceId/bulk-rewrite`), progress bar, cancel | `useSeoEditorBulkWorkflow.ts:284-308,356-372`; UI `BulkOperations.tsx:211-236`; server `server/routes/webflow-seo-jobs.ts:62` | at_risk (mechanics) | Bulk bar "AI-fix missing" (`editor.js:819,1695-1698`) covers only missing fields, synchronous | Rewrite-all-selected (not just missing), job progress, and cancel have no visible home. |
| C6 | Persistent SEO Suggestions panel: bulk-rewrite output stored server-side as 3-variation suggestion rows; select per row, "Apply N to Webflow", "Dismiss All"; survives reloads | `SeoEditor.tsx:99-104`; `src/components/editor/SeoSuggestionsPanel.tsx:15-199`; server `server/routes/webflow-seo-suggestions.ts:35,44,76,175` | at_risk | — | No prototype equivalent; the review-then-apply persistence layer for bulk AI output would be silently lost. |
| C7 | Analyze All Pages / Analyze Remaining (N) → background `SEO_BULK_ANALYZE` job (`POST /api/seo/:workspaceId/bulk-analyze`) with progress + cancel + "N/M have analysis on file" | `useSeoEditorBulkWorkflow.ts:183-206,340-354`; UI `src/components/editor/SeoEditorTableControls.tsx:33-72`; server `webflow-seo-jobs.ts:27` | at_risk | — | Depends on Q2 (Page-Intelligence merge). |
| C8 | Background-job lifecycle: WS progress/complete/failed handlers (`BULK_OPERATION_PROGRESS/COMPLETE/FAILED`), job-id persistence + mount-time recovery of in-flight jobs, NotificationBell tracking | `useSeoEditorBulkWorkflow.ts:82-181` (WS via `useWorkspaceEvents`), `90-122` (mount recovery), `seoEditorPersistence.ts` job-id cache | at_risk | — | Platform contract (`docs/rules/background-generation.md`); invisible in a static prototype but non-negotiable for parity. |
| C9 | Apply bulk-rewrite preview rows sequentially to Webflow with per-page progress | `useSeoEditorBulkWorkflow.ts:310-338` | at_risk | — | Pairs with C5/C6. |
| C10 | Publish Site (site-wide Webflow publish, `POST /api/webflow/publish/:siteId`) | `SeoEditor.tsx:131-144`; button `SeoEditorHeaderActions.tsx:95-109`; server `server/routes/webflow.ts:250` | at_risk | Prototype publish is per-row / per-selection status flip (`editor.js:1687,1694`) | Whole-site publish button has no home. See Q5. |

### D. CMS editor

| # | Capability | Evidence (HEAD) | Status | Home in new IA | Notes |
|---|---|---|---|---|---|
| D1 | Collections list with items; expand/collapse collections + items; per-collection select-all with "N selected" badge | `src/components/CmsEditor.tsx:38-46,198-231`; `src/components/cms-editor/CmsEditorCollections.tsx` (select-all at 160-164 area, per `useCmsEditorApprovalWorkflow.ts:82` `toggleSelectAllInCollection`) | preserved | CMS section rows + group select (`editor.js:790-801`) | |
| D2 | Edit CMS item Name + detected SEO title/description fields; save as **draft** via `PATCH /api/webflow/collections/:collectionId/items/:itemId` | `src/components/cms-editor/useCmsEditorSaveWorkflow.ts:42`; field detection `cmsEditorModel.ts` (`getExtraSeoFields`/`getTitleAndDescriptionFields`, used at `seoWriteTargetResolver.ts:77-78`) | preserved | CMS row edit; slug locked to CMS field (`editor.js:910-912`) | |
| D3 | Publish collection (publishes saved item ids) `POST /api/webflow/collections/:collectionId/publish` — draft→live gating | `src/components/cms-editor/useCmsEditorPublishBulkWorkflow.ts:54-75` | preserved | "Save & publish" per CMS item (`editor.js:782,1046`) — publish-gated per collection per section subtitle (`editor.js:793`) | Prototype collapses to per-item; per-collection batch publish of N saved items must remain possible. |
| D4 | CMS AI rewrite: single field, paired both-mode, apply single/paired variation | `CmsEditor.tsx:98-112`; `useCmsEditorAiWorkflow.ts:107,160` (same `/api/webflow/seo-rewrite`) | preserved | Same AI affordances on CMS rows | Variation picking → same risk as B7. |
| D5 | CMS bulk AI rewrite over selection with 4 target modes (Names / Titles / Descriptions / All SEO fields), auto-expand during processing, progress | `CmsEditor.tsx:114-133` (`useCmsEditorPublishBulkWorkflow.bulkAiRewrite`); modes per `FEATURE_AUDIT.md:4636` | at_risk | Bulk bar AI-fix covers missing only | The Name-rewrite mode especially has no prototype home. |
| D6 | CMS send-to-client approval workflow (per-item selection across collections) + approval error surface | `useCmsEditorApprovalWorkflow.ts:72` (POST `/api/approvals/:workspaceId`); `CmsEditor.tsx:75-91` | preserved | Bulk bar Send (`editor.js:821`) | |
| D7 | Per-item approval history ("N changes in history" expandable, built from approval batches) | `CmsEditorCollections.tsx:436-444`; `buildItemApprovalMap` `CmsEditor.tsx:93-96` | at_risk | — | No prototype equivalent. |
| D8 | Item issue highlighting: color-coded borders, "No title"/"No desc" badges, collection-header aggregate issue counts, char-count color coding, unsaved blue badges, untitled-in-red | `FEATURE_AUDIT.md:2608-2615`; render in `CmsEditorCollections.tsx` | improved | Sheet validity classes + flags (`editor.js:1118,1155-1176`) | |
| D9 | Missing-SEO-fields tip (collections without dedicated SEO fields → suggest adding in Webflow schema) | `CmsEditor.tsx:233-247` | at_risk | — | Small but real guidance; no home. |
| D10 | CMS slug/name write guards + static-only endpoint protections | `docs/rules/seo-editor-write-targets.md:9-11`; `FEATURE_AUDIT.md:5036` ("SEO Editor CMS Write Guards & Static-Only Endpoint") | preserved | Enforced server-side; prototype respects it (CMS slug disabled, `editor.js:911`) | |

### E. Approvals, tracking, cross-system wiring

| # | Capability | Evidence (HEAD) | Status | Home in new IA | Notes |
|---|---|---|---|---|---|
| E1 | Unified "Sent to Client" pending-approvals panel (SEO-filtered) listing batches with status, expandable item details, **Retract** (with inline confirm) incl. approved/rejected batches; auto-refresh; invalidates page-edit states on retract | `SeoEditorWrapper.tsx:118-125,58-65`; `src/components/PendingApprovals.tsx:2,46-58,126-132,169-172` | at_risk | — | Prototype flips a row to "In review" but has no batch panel and no retract. Retract is a shipped, client-visible workflow (`FEATURE_AUDIT.md:1826`). |
| E2 | Page edit-state tracking: 7-state model `clean / issue-detected / fix-proposed / in-review / approved / rejected / live` with colored borders + badges, tracking summary bar with per-status counts | `shared/types/workspace.ts:288`; `src/hooks/usePageEditStates.ts:48-76`; `src/components/editor/SeoEditorTrackingSummary.tsx:1-65`; row border `PageEditRow.tsx:72,78,118` | at_risk (partially) | Prototype status model is `draft / sent / approved / live` (`editor.js:674`) | `rejected`, `issue-detected`, `fix-proposed` states have no prototype rendering. See Q4. |
| E3 | Clear tracking per page; Reset all tracking (`POST /api/workspaces/:id/page-states/clear`, `workspaces.deletePageState`) | `SeoEditor.tsx:146-150`; `useSeoEditorBulkWorkflow.ts:374-387`; UI `PageEditRow.tsx:119-130`, `SeoEditorTrackingSummary.tsx:26-35,53-62` | preserved | "Reset tracking" header link (`editor.js:1329,1716-1721`) | Per-page clear needs a row affordance. |
| E4 | Live WS updates for approvals (client actions reflect in admin in real time: `approval:update` / `approval:applied`) | `FEATURE_AUDIT.md:1826` (PendingApprovals wires `useWorkspaceEvents`); data-flow rule CLAUDE.md | preserved | Mutation/feedback contract in Build Conventions | Must be wired in rebuild, per data-flow rules. |
| E5 | SEO change tracking on every applied change (editor save, bulk fix, approval apply) → `seo-changes` log powering `SeoChangeImpact` GSC before/after on Workspace Home | server `server/routes/seo-change-tracker.ts:24,34`; `FEATURE_AUDIT.md:3249` | preserved | Server-side; survives as long as rebuild writes through the same endpoints | Regression risk only if the rebuild bypasses `PUT /api/webflow/pages/:pageId/seo` etc. |
| E6 | Unsaved-changes banner ("Save individual pages then publish to go live") | `src/components/editor/SeoEditorWorkflowPanels.tsx:47-51` | preserved | Dirty flags + footer status (`editor.js:737,779,1042`) | |
| E7 | Permissions/auth: admin HMAC-gated workspace routes; all editor endpoints behind `requireWorkspaceSiteAccess`/`requireWorkspaceAccess` | e.g. `server/routes/webflow-seo-jobs.ts:27,62`; `webflow-seo-apply.ts:29,40`; `webflow.ts:192,250` | preserved | Same server contracts | No tier gates on this admin surface (TierGate is client-side; none found in editor components). |
| E8 | No `?tab=` deep link on this Page (single-surface; deep-linking is via fixContext, B14) | `src/routes.ts:5`; no `useSearchParams` in `SeoEditor*`/`CmsEditor*` (grep) | preserved | n/a | If the rebuild adds Edit/Research modes, a `?tab=`/mode param would be NEW and must follow the two-halves contract. |

### F. Prototype-only proposals (new functionality — needs sign-off)

| # | Proposal | Prototype evidence | Notes / dependency |
|---|---|---|---|
| N1 | Worksheet spreadsheet layout: 6-column grid (select / page / keyword-match / title / meta / score) with Enter/Shift-Enter cell navigation | `editor.js:341-420,1620-1628` | Pure UX upgrade over accordion rows. |
| N2 | On-page optimization score per page (0–100 ring + checklist: title/desc length, kw in title/desc/H1/slug, location signal) with "Fix →" focus jumps | `editor.js:629-668,862-878` | HEAD has optimizationScore only inside persisted page analysis (`FEATURE_AUDIT.md:4471`); a live client-side score is new. Needs a scoring-source decision (client heuristic vs `keywords.analyze`). |
| N3 | H1 editing and URL-slug editing ("changing a live slug sets up a redirect automatically") | `editor.js:904-913` | **No HEAD API writes H1 or slug.** New Webflow write scope + automatic-redirect behavior — significant new server work; must be explicitly approved. |
| N4 | Review Queue mode: keyboard triage (A approve / R request changes / S send / P publish / ←→ nav) over all pending targets with progress rail | `editor.js:1068-1115,1540-1554,1722-1735` | New workflow; "Approve" semantics conflict with HEAD where approval is a CLIENT action (see Q4). |
| N5 | Quick-filter chips: Need work (<55 score), Missing tags, Keyword gap (kw not in title/meta), In progress, Noindex | `editor.js:703-710,1343-1350` | Needs `index/noindex` data per page (not currently in `PageMeta` — new data requirement) and N2's score. |
| N6 | Per-row rank position + monthly organic traffic + last-edited display | `editor.js:508,729-730,1018` | HEAD editor rows don't show GSC/rank data; would need a data join (audit-traffic or rank-tracking APIs exist elsewhere). |
| N7 | Inline title rename via double-click in the list | `editor.js:739-742,1652-1659` | |
| N8 | Assign / change / clear target keyword and "Track keyword" directly from the editor's Research drawer | `editor.js:983-991,1592-1595` | Writes to strategy pageMap / tracked keywords — cross-surface write; Keyword Hub owns this today. |
| N9 | "Add redirect" CTA on manual (read-only) rows | `editor.js:774-775,932` | Hand-off to Redirects tooling; additive. |
| N10 | Edit ⇄ Research mode toggle folding Page Intelligence (`page-intelligence` Page, `navRegistry.tsx:139`) into the SEO Editor; docked deep-edit SERP strip; pinned-wide research drawer | `editor.js:1510-1528,1225-1292`; Parity Ledger row "Page Intelligence → SEO Editor (Research mode), status: improved" | IA consolidation affecting another surface — coordinate with the Page Intelligence auditor. See Q2. |
| N11 | Graduation note: publishes show in admin cockpit, not client portal, until ranking moves ("graduates into the Insights Engine") | `editor.js:947-950,964` | New cross-surface narrative contract; needs Insights-Engine-side support. |
| N12 | Bulk bar admin "Approve" action | `editor.js:820,1692` | Conflicts with HEAD approval semantics (client approves). See Q4. |

---

## 2. Prototype coverage notes

**Demonstrated (maps to HEAD):** three-source write model with counts and grouped sections; per-source select-all + multi-select + sticky bulk bar; missing-tags fix banner; search; collection dropdown; character counters with good/warn/over color bands; SERP + Facebook previews (live-updating); send-to-client / publish / save-draft lifecycle per row and in bulk; reset tracking; read-only manual rows with reasons; CMS slug locked; AI field-level and set-level generation (purple, admin-only); manual-row exclusion from every bulk path.

**Omitted (drives the at_risk rows above):** pattern apply (C4); persistent AI-suggestions review panel (C6); background-job mechanics — progress, cancel, mount recovery, WS events (C5/C7/C8/C9); Analyze/persist page analysis (B9/C7); AI 3-variation and paired pickers (B7/D4); pending-approvals panel with Retract (E1); the 3 extra page-edit states (E2); per-item CMS approval history (D7); CMS 4-mode bulk rewrite incl. Names (D5); site-wide Publish (C10); fixContext inbound deep-link (B14); session persistence (B15); recommendations-engine flags (B11/B12); error states (B5); OG mirroring on save (B3); CMS missing-SEO-fields tip (D9).

**New proposals:** N1–N12 above; all need owner sign-off, N3 (H1/slug writes + auto-redirect) and N4/N12 (admin approve semantics) being the heaviest.

## 3. Parity Ledger reconciliation

The Platform Parity Ledger row for this surface (`Platform Parity Ledger.html`, Optimization → "SEO Editor", `comp:'SeoEditor · seo-editor'`) is **status: improved** with all six sub-tools marked `present` (title/meta/OG editor, source model, bulk-scan spreadsheet, research drawer, CmsEditor, send-to-client/approve/publish). **No Gap or Partial rows exist for SEO Editor** — nothing to resolve, but this audit finds the ledger's "present" grading optimistic at the mechanics level (the ledger tracks features, not the C4–C9/E1/E2 mechanics above; those are the real parity debt). Related ledger row: "Page Intelligence" → `home:'editor.js (Research mode)', to:'SEO Editor', status:'improved'` — this surface inherits that merge (Q2); the Architecture tab explicitly moves to Links (not this surface's concern).

## 4. Trade-offs — quick win vs full implementation

| Item | Quick win | Full version | Risk of quick win |
|---|---|---|---|
| Worksheet grid (N1) | Ship the spreadsheet layout over the existing `useSeoEditor`/`useCmsEditor` data + existing save/AI endpoints; keep accordion drawer for detail | Full master-detail + docked SERP strip + pinned research drawer | Low — same data contract; must keep per-row selection driving the existing bulk hooks. |
| On-page score (N2) | Client-side heuristic score exactly as prototyped (pure function of title/desc/kw fields) | Score sourced from persisted page analysis (`keywords.analyze`) so editor + Page Intelligence agree | Two scores that disagree = trust landmine; label the heuristic clearly or defer until analysis-backed. |
| Bulk AI rewrite | Keep HEAD's job-based bulk rewrite + suggestions panel behind the new bulk bar ("AI-fix missing" triggers `BULK_SEO_FIX`; "AI rewrite" triggers `SEO_BULK_REWRITE`) | Inline progressive per-row results in the worksheet (prototype feel) with job progress inline per cell | Quick win preserves C5–C9 wholesale; full version must re-implement cancel/recovery or lose them. |
| Review Queue (N4) | Queue over existing statuses with only Send-to-client + Publish + Skip (defer Approve/Request-changes) | Full A/R/S/P triage once approval semantics are decided (Q4) | Shipping admin "Approve" early would corrupt the client-approval state machine. |
| Page Intelligence merge (N10/Q2) | Keep `page-intelligence` Page live; add a Research drawer in the editor that reads the same persisted analysis | Retire the standalone Page and fold analyze-all + persistence + brief/schema/traffic hand-offs into Research mode | Premature retirement of the Page breaks its inbound links (nav, command palette, fix-routing) — route-removal checklist applies. |
| Rank/traffic per row (N6) | Show strategy keyword + persisted `optimizationScore` only (data already joined via `usePageJoin`) | Join GSC clicks + rank-tracker positions per row | Extra API fan-out per page list render; needs the Data-Source Ledger fan-out audit. |
| Manual rows | Keep read-only listing + reason (parity) | N9 "Add redirect" hand-off into Redirect manager with prefilled source URL | None — additive. |
| H1/slug editing (N3) | Do not ship in v1; keep fields visible read-only ("edit in Webflow") | New Webflow write endpoints + slug-change redirect automation | Shipping the input without the write path = silent data loss illusion; hard stop until API exists. |

## 5. Open questions (stop-and-ask — owner sign-off required)

- **Q1 (N3):** H1 + URL-slug editing with "automatic redirect on slug change" is prototype-new and has **no server API at HEAD**. Approve as a new feature ticket (Webflow write scope + redirect automation), or render read-only in v1?
- **Q2 (B9/C7/N10):** The ledger merges Page Intelligence into SEO Editor Research mode. Does the rebuilt editor own (a) per-page Analyze + auto-persist to `keywordStrategy.pageMap`, (b) Analyze-All background job, and (c) the "analysis on file" state? If Research mode ships without the persist path, AI rewrites lose their analysis context (`server/seo-context.ts` `buildPageAnalysisContext`, `FEATURE_AUDIT.md:4471`). Coordinate with the Page Intelligence surface audit.
- **Q3 (B11/B12):** Where do recommendations-engine metadata flags (traffic-at-risk, priority, rec-first sorting from `useRecommendations`) render in the new IA — folded into the on-page score, a row flag, or the Research drawer? Prototype's recommendations are seed heuristics, not the engine.
- **Q4 (E2/N4/N12):** Status-model conflict. HEAD: 7-state `PageEditState` where **approved/rejected are client actions** via the approvals pipeline. Prototype: 4-state `draft/sent/approved/live` with an **admin** Approve button (bulk bar + queue). Decide the canonical state machine and who may set `approved`; `rejected`, `issue-detected`, `fix-proposed` need explicit homes or an approved deprecation.
- **Q5 (C10):** Site-wide "Publish Site" button — keep on the editor, move to a global action, or drop in favor of per-target publish only? (Per-target publish alone cannot flush pending static-page changes site-wide the way `POST /api/webflow/publish/:siteId` does.)
- **Q6 (B4/C6/B15):** Persistence tier for drafts and AI output: HEAD has localStorage drafts + server-persisted suggestion rows + RQ session cache. Prototype implies a first-class "draft" status. Should the rebuild move draft persistence server-side (upgrading B4/B15), and does the suggestions panel (C6) survive as-is, merge into the queue, or get replaced — and if replaced, with what persistence?
- **Q7 (E1):** The pending-approvals "Sent to Client" panel with **Retract** has no prototype home. Where does batch visibility + retract live in the new IA (editor panel, Engine/queue surface, or global approvals view)? It must exist somewhere — it's the only admin path to withdraw a batch from a client.
- **Q8 (N5):** Noindex quick-filter requires per-page index status, which `PageMeta` does not carry today (`src/hooks/admin/useSeoEditor.ts`). Approve the data addition (likely from audit/crawl data) or drop the chip.
- **Q9 (N8):** May the editor WRITE target keywords (assign/change/clear + "track") directly, given Keyword Hub / strategy owns keyword-set mutations (`docs/rules/strategy-recommendations.md`, dedicated `strategy_keyword_set` table)? Needs a single-writer decision.

---
*Audit method: full read of `SeoEditorWrapper.tsx`, `SeoEditor.tsx`, `CmsEditor.tsx`, all 18 files in `src/components/editor/`, CMS-editor hooks, `PendingApprovals.tsx`, server routes (`webflow-seo-*.ts`, `webflow.ts:192/250`, `seo-change-tracker.ts`), `usePageEditStates`, `fix-context.ts`, `seo-editor-write-target` resolver; FEATURE_AUDIT.md grep (rows 1814, 2608, 3249, 4020, 4163, 4471, 4482, 4493, 4569, 4636, 5036); full read of prototype `mockup/editor.js`; Platform Parity Ledger SEO Editor + Page Intelligence rows.*
