# Verified-Clean pr-check Rules

> Load-bearing allowlist of rules in `scripts/pr-check.ts` that currently
> report `✓` (zero matches) on a full-repo scan. Enforced by the
> `pr-check --all status parity with allowlist` meta-test in
> `tests/pr-check.test.ts`. See Round 2 Task P1.5 of the
> `2026-04-10-pr-check-audit-and-backfill` plan for the motivation.

---

## Why this file exists

A rule in `scripts/pr-check.ts` can silently fail in four ways:

1. **Category A (file-list)** — `resolveCheckFileList` applies a filter that
   excludes all files before `customCheck` runs, so the callback sees an
   empty array and returns zero hits.
2. **Category B (regex-too-narrow)** — the regex only matches one variant
   (e.g. single quotes) while real violations use the other (double
   quotes).
3. **Category C (parser-lite)** — a hand-rolled string scan (`search(/[{=]/)`,
   brace counting without depth tracking) truncates on a perfectly legal
   syntactic construct.
4. **Category D (shell-quoting)** — a regex with an embedded `"` or a
   lookbehind/lookahead collides with the outer `grep -E "${pattern}"`
   shell invocation. `grep` errors, `|| true` swallows it, the runner
   reports `✓`.

A `✓` in `pr-check --all` output means one of:

- the rule is genuinely finding zero violations (codebase is clean), or
- the rule is silently broken.

This file pins the first case. Every entry is a rule that has been
**manually spot-checked** to confirm its pattern is reachable, its shell
invocation runs without error, and its `✓` reflects a genuinely-clean
codebase.

If a rule moves from `⚠`/`✗` back to `✓` after a backfill, add it here in
the same commit. If a new rule lands at `✓` on day one, either backfill a
fixture that forces a positive match (preferred) or add it here with an
explicit justification.

---

## How to add a rule to this file

1. Run `npx tsx scripts/pr-check.ts --all` and confirm the rule reports `✓`.
2. Run the rule's regex manually against the repo to confirm the shell
   invocation succeeds (no `grep: repetition-operator operand invalid`,
   no `sh: syntax error near unexpected token`, no exit-code-2 errors).
   For customCheck rules, confirm the callback is reachable on a
   representative file (write a minimal trigger and pass it via a
   fixture test).
3. Add an entry to the table below with: rule name (exact), verification
   method (`regex-shell`, `regex-manual`, `customCheck-fixture`,
   `backfill-complete`), and a one-line justification.
4. The P1.5 meta-test will fail until your entry matches the rule name
   exactly — that failure is the gate doing its job.

---

## Allowlist

| Rule Name | Verified By | Justification |
|-----------|-------------|---------------|
| Purple in client components | regex-shell | Simple literal `purple-`; grep succeeds; zero hits in `src/components/client/` confirmed. |
| Forbidden hues (violet/indigo) in components | regex-shell | Pattern `(violet-\|indigo-)`; grep succeeds; zero hits in `src/components/` confirmed via manual grep. |
| Bare JSON.parse on server | regex-shell | Pattern `JSON\.parse\(`; pathFilter `server/`; 22 exclusions for legitimate non-DB parse sites; zero remaining hits confirmed via manual grep. |
| Banned hedge words in briefing templates | regex-shell | Pattern `\b(potentially\|could\|may\|appears to\|suggests\|might\|seems)\b`; pathFilter `server/briefing-templates/`; new-with-feature (Phase 2.5a of client-insights-redesign) — the directory is created in the same PR, so it starts at zero by construction. Hatch line `// hedge-ok` for legitimate cited-source hedges. |
| Hard-coded studio name | regex-shell | Pattern `hmpsn[ .]studio`; two excludes (`server/constants.ts`, `src/constants.ts`); three excludeLines for SVG alt text; `tests/` dir in EXCLUDED_DIRS so test fixtures don't count; zero hits in scanned dirs. |
| formatBrandVoiceForPrompt reintroduction | regex-manual | Pattern `\bformatBrandVoiceForPrompt\b`; verified both directions: (a) `grep -rn --include='*.ts' --include='*.tsx' -E '\bformatBrandVoiceForPrompt\b' server/ src/` returns zero hits (the function was deleted in PR #168); (b) regex matches the synthetic trigger `const x = formatBrandVoiceForPrompt("test");`. Excludes cover `tests/` (legitimate "this was deleted" comments), `.codesight/` (auto-generated), and `scripts/pr-check.ts` (the rule itself references the name in its error message). Do-not-reintroduce rule; the only way this flips from ✓ to a match is if someone re-adds the helper, which is exactly what we want to block. |
| Unguarded recordAction() call | regex-shell | Pattern `recordAction\s*\(\s*\{`; pathFilter `server/`; `server/outcome-tracking.ts` excluded as the definition site; zero remaining hits confirmed via manual grep. |
| Local prepared statement caching | regex-shell | Literal `let stmt`; grep succeeds; zero hits outside `server/db/`. |
| z.array(z.unknown()) on server | regex-shell | Fully-escaped literal `z\.array\(z\.unknown\(\)\)`; exclude list `server/db/json-validation.ts`; grep succeeds. |
| Bare SUM() without COALESCE in db.prepare | regex-shell | Pattern `(^\|[^(])SUM\(`; standard alternation; grep succeeds; zero hits outside `server/`. |
| as any on dynamic import results | regex-shell | Pattern `(\([a-z]+:\s*any\)\|as any[);,.])`; standard alternation with char class; grep succeeds. |
| Hardcoded dark hex in inline styles | regex-shell | Pattern `style=\{[^}]*(#0f1219\|...)`; brace char class; grep succeeds; only Styleguide.tsx (excluded) would match. |
| SVG with hardcoded dark fill/stroke | customCheck-fixture | Converted from regex-shell; original pattern contained `\"` which the safePattern escaping double-escaped to `\\"`, breaking the shell command. JS regex avoids the shell entirely. |
| Background generation in high-churn routes must be allowlisted | customCheck-fixture | Custom rule has trigger, multiline trigger, hatch, awaited-generation negative, canonical jobs-route negative, site-specific allowlist, and site-level legacy hatch fixtures in `tests/pr-check.test.ts`. Full-repo scan reports zero unallowlisted detached generation sites. |
| Direct listPages() outside workspace-data | regex-shell | Literal function name; grep succeeds; workspace-data.ts and webflow-pages.ts are excluded as the only legitimate call sites. |
| Direct buildSeoContext() call | regex-shell | Literal function name; grep succeeds. |
| buildWorkspaceIntelligence() without slices (assembles all 8 slices) | regex-shell | Pattern `buildWorkspaceIntelligence\(` with `excludeLines` for `slices:` and shorthand slice forwarding. Full-repo scan reports ✓ after intelligence callers were migrated to explicit slice selection; fixture coverage for related slice/section consistency lives in `tests/format-for-prompt.test.ts`. |
| formatForPrompt with inline sections literal (use buildIntelPrompt or sections: slices) | regex-shell | Literal function name with nested match; grep succeeds. |
| Placeholder test assertion — expect(true).toBe(true) | regex-shell | Fully-escaped literal; grep succeeds; `tests/` pathFilter correctly opted in after the EXCLUDED_DIRS fix. |
| Source-sniffing in tests (readFileSync on .ts/.tsx source) | regex-shell | Pattern `readFileSync\(.*\.(ts\|tsx)`; grep succeeds; intentional endpoint migration guards must use `// readFile-ok` on the source-read line. Current migration contract tests use those hatches explicitly, so full-repo scan reports ✓. |
| Bare JSON.parse on DB row column | regex-shell | Literal `JSON\.parse\(row\.`; grep succeeds; json-validation.ts and migrate-json.ts are excluded. |
| Unguarded SET status = ? (state machine transition) | regex-shell | Pattern `SET\s+(status\|batch_status)\s*=\s*[?@]`; standard alternation; grep succeeds. |
| Raw bulk_lookup string outside keywords type file | regex-shell | Literal `'bulk_lookup'`; grep succeeds. |
| Raw ai_estimate string in server files | regex-shell | Literal `'ai_estimate'`; grep succeeds. |
| replaceAllPageKeywords called outside keyword-strategy route | regex-shell | Literal function name; grep succeeds. |
| getBacklinksOverview called outside workspace intelligence SEO context | regex-shell | Literal function name; grep succeeds. |
| Silent bare catch in server files | regex-shell | Pattern `\} catch \{$`; end-anchor; grep succeeds. Expanded from workspace-intelligence.ts to all server/ files after #576 broad catch hardening. |
| useGlobalAdminEvents import restriction | customCheck-fixture | P1.1 Round 2 fix — converted to customCheck with dual-quote regex and inline+above-line hatch. 6 fixture tests cover all paths. |
| getOrCreate* function returns nullable | customCheck-fixture | P1.2 Round 2 fix — depth-tracked `findReturnRegionEnd()` walker replaced fragile `.search(/[{=]/)`. 11 fixture tests cover object-literal, Promise, Array, arrow, intersection, and non-null return shapes. |
| Record<string, unknown> in shared/types | regex-shell | Pattern `Record<string,\s*unknown>`; grep succeeds. |
| PATCH spread without nested merge | regex-shell | Pattern `\.\.\.(existing\|current),\s*\.\.\.req\.body([^.\w]\|$)`; standard alternation; grep succeeds. |
| Assembled-but-never-rendered slice fields | customCheck-fixture | PR #168 I17 migration from inline block. Pure helper `findUnrenderedSliceFields()` extracted so the harness can exercise it without monkeypatching ROOT. 5 describe tests cover trigger (widgetCount unreferenced), dot-access negative, bracket-access negative, missing-formatter short-circuit, and empty-input short-circuit. |
| callCreativeAI without json: flag in files that use parseJsonFallback | customCheck-fixture | PR #168 I17 migration from inline block. customCheck walks each `callCreativeAI(` call and flags any whose argument block lacks `json:` in files that also use `parseJsonFallback`. 4 describe tests cover trigger, `json: true` negative, no-`parseJsonFallback` negative, and mixed (some safe, one unsafe) trigger. |
| requireAuth in brand-engine route files (should be requireWorkspaceAccess) | customCheck-fixture | PR #168 I17 migration from inline block. Basename-matched against `BRAND_ENGINE_ROUTE_BASENAMES` (6 brand-engine route filenames) for harness testability. Regex is `\brequireAuth\b` with import-line and JSDoc-line exclusions. 5 describe tests cover trigger, inline `// auth-ok` hatch, above-line `// auth-ok` hatch, non-brand-engine route negative, and `requireWorkspaceAccess` negative. |
| useEffect external-sync dirty guard against the live prop | customCheck-fixture | PR #168 I17 migration from inline block. customCheck walks every useEffect body, finds `if (!isDirty) setX(...)` guards, and flags when `isDirty` is defined against the live prop (not a useRef). Uses `hasHatch()` so both inline and above-line `// sync-ok` comments suppress. 4 describe tests cover trigger, inline hatch, above-line hatch, and ref-based negative. |
| Constants in sync (STUDIO_NAME, STUDIO_URL) | customCheck-fixture | PR #168 I17 migration from inline block. Pure helper `compareStudioConstants()` extracted so the harness can exercise it without monkeypatching ROOT. 5 describe tests cover STUDIO_NAME drift, STUDIO_URL drift, both-drift, both-match negative, and empty-input short-circuit. |
| Untyped dynamic import (missing import type) | backfill-complete | 44 violations fixed in B6 backfill task: `import type * as ModName` added at file top for each imported module; `: typeof ModName` annotation added at each `await import()`; `// dynamic-import-ok` comment on every typed line; sharp module typed via `import type { default as SharpConstructor } from 'sharp'`. Rule now reports ✓ on full-repo scan. |
| UPDATE/DELETE missing workspace_id scope | backfill-complete | 39 violations fixed in B8 backfill task. Default fix threaded `workspaceId` through callers and added `AND workspace_id = ?` to UPDATE/DELETE WHERE clauses (defence-in-depth multi-tenancy). UUID-unique row IDs (`randomUUID()`, `crypto.randomBytes(32)`) and global retention sweeps (`cleanupOldSends`, `pruneOldest`, `deleteExpiredTokens`, etc.) hatched with `// ws-scope-ok` and an inline justification comment. Rule now reports ✓ on full-repo scan. |
| Vacuous .every() in tests (no length guard) | backfill-complete | 91 violations fixed in B5 backfill task. Default fix wrapped each `arr.every(predicate)` in `arr.length > 0 && arr.every(predicate)` so the assertion fails on an empty array instead of vacuously passing. `// every-ok` hatches: `tests/integration/client-signals-routes.test.ts:57` (vacuous-truthy intentional), `tests/unit/approvalsFilter.test.ts:26` (`isReady` helper — length guard is `b.items.length > 0` on the preceding line of a multi-line `&&` chain), and 3 assertion-level hatches in the same file (length-guarded by prior `toHaveLength` assertions). Rule reports ✓ on full-repo scan. |
| window.confirm() in client components | regex-shell | Rule added in 2026-04-20 light-mode overhaul PR (#240). The three `window.confirm(` calls that previously existed in `src/components/client/ApprovalsTab.tsx` were replaced with the new `ConfirmDialog` primitive in the same PR. Pattern `window\.confirm\(`; pathFilter `src/components/client/`. Manual `grep -rn "window\.confirm(" src/components/client/` returns zero hits. Rule reports ✓ on full-repo scan. |
| Raw fetch() in components | customCheck-fixture | Keyword strategy background-job migration (2026-05-04) removed the final unhatched component-level `fetch(` caller. The rule is intentionally implemented as an in-process JS regex because BSD `grep -E` cannot handle the original lookbehind. Fixture coverage in `tests/pr-check.test.ts` covers bare `fetch()` trigger, `.fetch()` method negative, `refetch()` negative, out-of-scope path negative, and inline/preceding-line `// fetch-ok` hatches. Full-repo scan reports ✓; the remaining client `fetch(` in `client/ContentTab.tsx` has an inline `// fetch-ok` because it fetches HTML for a new-window preview. |
| Public-portal mutation without addActivity | customCheck-fixture | B9 polish surfaced a single false-positive: the onboarding handler at `server/routes/public-portal.ts:83` calls `addActivity()` 101 lines below its `router.post(...)` opener. The rule's `PUBLIC_PORTAL_ROUTE_BODY_LOOKAHEAD` was 60 lines, silently truncating the scan window before reaching the call. B9 bumped the constant to 250 lines (defensive cap; routes in the wild range 5–110 lines). 4 fixture tests in tests/pr-check.test.ts cover trigger, inline hatch, above-line hatch, and addActivity-present negative. The rule's `pathFilter` was also removed in the same B9 polish: the customCheck self-filters via an explicit `endsWith('public-portal.ts')` guard, so the rule is no longer dependent on the file-list pipeline (Category A immunity). |
| Missing broadcastToWorkspace after DB write in route handler | customCheck-fixture | Client strategy feedback validation sprint (2026-05-05) removed the final unverified public-portal hatches by adding `WS_EVENTS.STRATEGY_UPDATED` broadcasts to keyword feedback, bulk keyword feedback, business priorities, content-gap vote, and matching admin keyword-feedback writes. Fixture coverage in `tests/pr-check.test.ts` covers a DB write trigger, `broadcastToWorkspace()` negative, `broadcast()` negative, and `// broadcast-ok` hatch behavior. `npx tsx scripts/pr-check.ts --all` now reports ✓ for the rule; remaining intentional non-broadcast writes carry explicit per-site hatches. |
| Raw string literal in broadcast() event arg | customCheck-fixture | PR #444 Webflow ownership hardening cleared the final legacy `broadcast('files:uploaded', ...)` hits by using intentional `// ws-event-ok` hatches for the global upload event. The rule is a customCheck, avoiding the original Category D shell-quoting failure around `['"]`. Existing fixture coverage in `tests/pr-check.test.ts` covers single-quoted and double-quoted triggers, `_broadcast()` private-wrapper negative, `broadcastToWorkspace()` negative, `ADMIN_EVENTS.*` negative, and inline/preceding-line `// ws-event-ok` hatches. `npx tsx scripts/pr-check.ts --all` now reports ✓ for the rule. |
| broadcastToWorkspace inside bridge callback | backfill-complete | 4 violations fixed in B9b backfill task. (1) `server/reports.ts:206` Bridge #12 (audit-page-health) and `:243` Bridge #15 (audit-site-health): both rewritten to return `{ modified }` from the async callback so `executeBridge()` auto-broadcasts `INSIGHT_BRIDGE_UPDATED` via the bridge-infrastructure auto-dispatch path; inline `broadcastToWorkspace()` removed; unused `WsEvents`/`Broadcast` type imports cleaned up. (2) `server/outcome-tracking.ts:189` Bridge #13 (action-annotation) and `server/routes/content-decay.ts:48` Bridge #2 (decay-suggested-brief): both hatched with `// bridge-broadcast-ok` because they dispatch domain-specific events (`ANNOTATION_BRIDGE_CREATED`, `SUGGESTED_BRIEF_UPDATED`) with custom payloads that the auto-dispatch path can't carry — the auto path always emits `INSIGHT_BRIDGE_UPDATED` with a `{ bridge }` body, but these handlers need richer payloads (`date`, `label`, `count`). The inline broadcasts are intentional and correct. Rule now reports ✓ on full-repo scan. |
| Global keydown missing isContentEditable guard | backfill-complete | 2 violations fixed in B9b backfill task. (1) `src/components/CommandPalette.tsx:106` hatched with `// keydown-ok` and a 12-line rationale comment block: the Cmd/Ctrl+K toggle must fire from input fields (Slack/Linear/Notion convention), and the Escape branch is gated on `open === true` with focus captured by the modal's own input — adding the standard `isContentEditable` guard would break both behaviours. (2) `src/components/NotificationBell.tsx:23` fixed with the canonical guard pattern (`HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement | isContentEditable` early-return) since the drawer can be opened via mouse click while focus is still in an unrelated input elsewhere. Rule now reports ✓ on full-repo scan. |
| Multi-step DB writes outside db.transaction() | backfill-complete | 2 violations fixed in B9b backfill task. (1) `server/routes/keyword-strategy.ts:2018` strategy-history INSERT + prune-DELETE wrapped in `db.transaction(() => { ... })` to make the save atomic; without it, an INSERT that succeeds followed by a DELETE that fails would leave the table over-quota and corrupt history ordering on the next generation. The narrowing of `ws.keywordStrategy` was hoisted into a local `previousStrategy` const so the closure preserves the type narrowing across the transaction boundary. (2) `server/routes/public-portal.ts:592` content_gap_votes if/else (DELETE for clear, INSERT/UPDATE for set) wrapped in a single `db.transaction(() => { ... })` — even though the two branches are mutually exclusive (only one runs per request), the transaction adds defence-in-depth so any future expansion of either branch (e.g. an audit-log INSERT) inherits atomicity automatically. Rule now reports ✓ on full-repo scan. |
| AI call before db.prepare without transaction guard | backfill-complete | 1 violation hatched in B9b backfill task. `server/voice-calibration.ts:264` `generateCalibrationVariations()` flagged because it `await`s `callCreativeAI()` and then runs `stmts().insertSession.run(...)`. Hatched with `// ai-race-ok` plus a 6-line rationale comment: this handler is provably single-writer per request — each call generates a fresh `cal_<randomUUID>` primary key AFTER the AI returns, with no existence check beforehand. Two concurrent requests create two distinct sessions (different random IDs), so there is no shared natural-key INSERT to race on. Sessions are intentionally append-only per call. Rule now reports ✓ on full-repo scan. |
| Admin mutation on client_users missing expectedWorkspaceId param | customCheck-fixture | Post-PR #168 scaled-review cleanup (2026-04-11). customCheck walks `export function update*\|delete*\|change*` declarations in `server/client-users.ts`, parses the multi-line param list with paren-depth + string-literal tracking (`PARAM_LIST_MAX_SPAN = 20`), and flags any signature missing `\bexpectedWorkspaceId\b`. Hatch is `// ws-authz-ok` on the declaration line or the line immediately above. 8 fixture tests in tests/pr-check.test.ts cover: single-line trigger, multi-line trigger, `change*` verb trigger, inline hatch, above-line hatch, multi-line negative with the param present, non-mutation verb (`recordClientLogin`) negative, and out-of-scope file negative. Rule already ✓ at ship time — the 3 existing mutations (`updateClientUser`, `changeClientPassword`, `deleteClientUser`) all carry the param from PR #168 commit 293485d; this rule exists to catch a future mutation function added without the guard. |
| Inline voice-profile authority check (use isVoiceProfileAuthoritative helper) | regex-manual | Post-PR #168 scaled-review cleanup, retargeted during SEO context retirement. Pattern `voiceProfileBlock\.length`; verified manually that the only real hit is the canonical helper body in `server/intelligence/seo-context-source.ts`, suppressed by `// voice-authority-ok`. Do-not-reintroduce rule scoped to the owned SEO context source; any future inline authority check must route through `isVoiceProfileAuthoritative(profile, voiceProfileBlock)`. |
| Bare brand-engine read in seo-context source (use safeBrandEngineRead) | customCheck-fixture | SEO context retirement retargeted the rule from the deleted builder to `server/intelligence/seo-context-source.ts`. customCheck scans for `\b(getVoiceProfile\|listBrandscripts\|listDeliverables)\s*\(`, skipping imports, JSDoc lines, and comments; a call is wrapped only when `safeBrandEngineRead(` appears earlier on the same line. Hatch is `// safe-read-ok`. 9 fixture tests cover triggers, hatches, wrapped negative, comment/JSDoc negatives, and out-of-scope negative. Full-repo scan reports ✓ with `getVoiceProfile` reads wrapped in the source module. |
| Test body has no assertion or explicit failure throw | customCheck-fixture | 2026-04-11 test audit stop-gap. customCheck first masks string literals, line/block comments, and template literal contents (preserving `${...}` interpolations) via a position-preserving `maskNonCode()` helper so line numbers stay aligned with the raw file for error reporting. It then regex-matches `(^|[^\w.$])(it\|test)\s*\(` on the masked content, brace-walks each matching test body (up to `}` at depth 0), and checks for any of 23 assertion tokens: `expect(`, `assert(`, `.toBe`, `.toEqual`, `.toMatch`, `.toThrow`, `.toHaveLength`, `.toContain`, `.toHaveBeenCalled`, `.toHaveProperty`, `.toBeDefined`, `.toBeUndefined`, `.toBeNull`, `.toBeTruthy`, `.toBeFalsy`, `.toBeGreaterThan`, `.toBeLessThan`, `.toBeInstanceOf`, `.rejects`, `.resolves`, `throw new Error`, `throw new TypeError`, `throw new RangeError`. Scoped to `*.test.ts` / `*.test.tsx`; `/e2e/` paths are excluded because Playwright action-throws (`page.click`) replace explicit assertions. A `(i === 0 \|\| src[i-1] !== '\\')` guard on both `//` and `/*` comment detection prevents regex literals like `/\/assets\//` (used by `mockWebflowSuccess()` in `webflow-id-semantics.test.ts`) from being mis-parsed as line comments — the bug that caused 4 false positives during rule development. Hatch is `// no-assertion-ok` on the `it(` line or the line immediately above, with a one-line rationale naming the helper the body delegates to. 12 fixture tests cover: `it(...)` trigger, `test(...)` trigger, expect() negative, .toEqual negative, .rejects negative, `throw new Error` negative, inline hatch, above-line hatch, non-test file filter, Playwright e2e exclusion, multi-`it` partial flagging, and the string-literal masking regression. 13 real helper-delegation sites hatched in the same commit: 6 in `tests/unit/format-edge-cases.test.ts` delegating to `noGarbage()` (3 `expect().not.toMatch(...)` per call) and 7 in `tests/integration/content-lifecycle.test.ts` delegating to `walkStatuses()` (4 `expect(...)` per transition step). Catches the silent-pass class that the 2026-04-11 test audit found in 3 stripe webhook/config test bodies. |
| seo-context.ts import restriction (retired module) | customCheck-fixture | SEO context retirement hardened the old deprecation rule into a no-import rule for production server code. customCheck catches static and dynamic imports whose module path ends in `seo-context` or `seo-context.js`, deliberately avoiding `seo-context-source.js`. No hatch is allowed. Fixture tests cover single-quote trigger, double-quote trigger, hatch-is-ignored trigger, dynamic-import trigger, and clean-file negative. Full-repo scan reports ✓ after deleting `server/seo-context.ts` and migrating callers to workspace intelligence / prompt-rich-blocks / owned source modules. |
| requireAuth usage outside allowed route files | customCheck-fixture | P0 expansion rule. customCheck scans all `server/**/*.ts` files for `\brequireAuth\b` usage, skipping imports, comments, and function definitions. Allowed basenames (`auth.ts`, `users.ts`) and brand-engine route basenames (covered by their own dedicated rule) are excluded. Hatch is `// auth-ok` on the flagged line or the line immediately above. 8 fixture tests in tests/pr-check.test.ts cover: trigger in non-allowed route, inline hatch, above-line hatch, routes/auth.ts negative, routes/users.ts negative, brand-engine route negative, server/auth.ts definition negative, import-only negative, and comment-only negative. Currently clean — only `routes/auth.ts` and `routes/users.ts` use `requireAuth`, both on the allowlist. |
| Duplicate globally-applied rate limiter in route file | customCheck-fixture | P0 expansion rule. customCheck scans `server/routes/**/*.ts` files for `\b(globalPublicLimiter\|publicApiLimiter\|publicWriteLimiter)\b` references, skipping comments. These three limiters are applied globally in `server/app.ts` to all `/api/public/` routes; re-applying them in a route file increments the same shared in-memory bucket twice, silently halving the effective rate limit. Hatch is `// limiter-ok` on the flagged line or the line immediately above. 8 fixture tests in tests/pr-check.test.ts cover: globalPublicLimiter trigger, publicApiLimiter trigger, publicWriteLimiter trigger, inline hatch, above-line hatch, aiLimiter negative (not globally applied), checkoutLimiter negative, and comment-only negative. Currently clean — no route file imports any of the three globally-applied limiters. |
| Port collision in integration tests | customCheck-fixture | P1 expansion rule. customCheck collects all `createTestContext(NNNN)` port allocations across every `*.test.ts` file in `tests/`, builds a port→usages map, and flags any port that appears in two or more files. Also flags ports outside the documented 13201–13319 range (per CLAUDE.md) as out-of-range violations, with a dedup guard to avoid double-flagging ports that are both duplicate and out-of-range. The pr-check test harness file itself (`pr-check.test.ts`) is excluded since its fixture strings contain `createTestContext()` literals that aren't real port allocations. Hatch is `// port-ok` on the `createTestContext()` line or the line immediately above. 6 fixture tests in tests/pr-check.test.ts cover: duplicate port trigger (both files flagged), inline hatch, above-line hatch, unique in-range ports negative, out-of-range port trigger, and no-createTestContext negative. Currently clean — every integration test file uses a unique port in the 13201–13319 range. |
| useGlobalAdminEvents called with workspace-scoped event name | customCheck-fixture | P2 expansion rule. customCheck scans `src/**/*.{ts,tsx}` files for `useGlobalAdminEvents({` call sites, then inspects handler keys for workspace-scoped event names (WS_EVENTS values parsed from `server/ws-events.ts`). Both string-literal keys (`'brandscript:updated'`) and computed keys (`[WS_EVENTS.BRANDSCRIPT_UPDATED]`) are detected. Hatch is `// global-events-ok` on the `useGlobalAdminEvents({` line or the line immediately above. 8 fixture tests in tests/pr-check.test.ts cover: string-literal WS_EVENTS value trigger, computed WS_EVENTS.* key trigger, ADMIN_EVENTS computed key negative, ADMIN_EVENTS string-literal value negative, `presence:update` negative (not a WS_EVENTS value), inline hatch, multiple workspace-scoped events trigger (2 hits), and no-useGlobalAdminEvents-call negative. Currently clean — the two audited global-fanout sites (`src/App.tsx`, `src/components/WorkspaceOverview.tsx`) only use ADMIN_EVENTS values and `presence:update`. |
| Discarded updatePageSeo return value | pattern-fixture | P1 Platform Health Sprint expansion rule. Pattern `await updatePageSeo\(` in `server/**/*.ts`; excludes lines containing `= await updatePageSeo(` (assigned) or `// seo-ok` (escape hatch). All 4 pre-existing call sites in `server/routes/webflow-seo.ts` already assign the return value; the one bare call in `server/routes/jobs.ts:467` was fixed in the same PR that added this rule (captured return value, checks `result.success`). 4 fixture tests in tests/pr-check.test.ts cover: bare `await updatePageSeo(` trigger, assigned return value negative, `// seo-ok` inline hatch negative, and pathFilter configuration assertion. Rule reports ✓ on full-repo scan. |
| Admin route mutation without addActivity | backfill-complete | 3 violations hatched in platform-health PR4: `router.post /api/webflow/keyword-feedback/:workspaceId`, `router.post /api/webflow/keyword-feedback/:workspaceId/bulk`, and `router.delete /api/webflow/keyword-feedback/:workspaceId/:keyword` in `server/routes/keyword-strategy.ts`. All three operate on transient approve/decline bookkeeping state (not workspace content) and were hatched with `// activity-ok: keyword approve/decline is transient feedback state, not a workspace activity event`. Rule now reports ✓ on full-repo scan. |
| Re-upsert without cloneInsightParams | customCheck-fixture | Platform Health Sprint PR 5a. The only violation (`server/analytics-intelligence.ts:1143` inside `enrichAndUpsert()`) was hatched with `// clone-ok` after verifying the callback is a fresh-insert path — `enrichAndUpsert` builds a newly-computed `upsertInsight` input from live analytics data rather than re-upserting an existing stored `AnalyticsInsight` row, so the field-drop risk the rule protects against does not apply. 3 fixture tests in `tests/pr-check.test.ts` (line 3681) cover: trigger (manual `insight.workspaceId` copy without spread), negative (`...cloneInsightParams(insight)` spread), and new-insight creation (no `insight.*` field-copy window). Rule reports ✓ on full-repo scan. |
| Inline React Query string key (use queryKeys.*) | backfill-complete | 6-phase querykey-factory migration (2026-04-15, PRs #208–#213) replaced every inline `queryKey: ['string-literal', ...]` in `src/` with calls to the centralized `queryKeys.*` factory in `src/lib/queryKeys.ts`. Final `grep -rn "queryKey: \['" src/` returns zero hits (only a JSDoc comment in `queryKeys.ts` itself). `npx tsx scripts/pr-check.ts --all` reports ✓ for this rule. |
| Raw provider date passed to new Date() | regex-manual | PR #218 A4 guard. Pattern `new Date\((\w+\.)?(first_?[sS]een\|last_?[sS]een\|last_?[vV]isited)` in `server/**/*.ts`; excludes `server/seo-data-provider.ts` (where normalizeProviderDate lives) and lines with `// provider-date-ok`. Verified manually: grep against server/ returns zero hits. The only legitimate conversion site is inside normalizeProviderDate itself, which is in the excluded file. Rule reports ✓ on full-repo scan. |
| Competitor keyword push missing serpFeatures | customCheck-fixture | PR #218 A3 guard. customCheck scans `server/**/*.ts` for `competitorKeywordData.push({` and looks ahead up to 12 lines for either `serpFeatures` (pass) or `});` without `serpFeatures` (flag). Hatch is `// compkw-serp-ok` on the push line or the immediately preceding line. The fix from Task 5 already includes `serpFeatures: ck.serpFeatures` in `server/routes/keyword-strategy.ts`. 5 fixture tests in tests/pr-check.test.ts cover: trigger (missing serpFeatures), negative (serpFeatures present), inline hatch, above-line hatch, and non-server file negative. Rule reports ✓ on full-repo scan. |
| Bare slug used in pagePath construction — use resolvePagePath(page) | customCheck-fixture | Slug-path hardening sprint (2026-04-21). customCheck scans `server/**/*.ts` and `src/**/*.ts` for `` `/${page.slug}` `` and `` `/${p.slug}` `` template literals, excluding `server/helpers.ts` and `src/lib/pathUtils.ts` (canonical implementations), lines with `.startsWith('/')` guards (ternary normalization), and lines referencing `publishedPath` (already-guarded fallbacks). Hatch is `// slug-path-ok`. All 15+ sites in the sprint were fixed to use `resolvePagePath(page)`. 8 fixture tests in `tests/pr-check.test.ts` cover: trigger (bare page.slug), trigger (p.slug variant), trigger (empty-fallback variant), negative (startsWith ternary), negative (publishedPath guard), inline hatch, above-line hatch, and canonical-implementation excludes. Rule reports ✓ on full-repo scan. |
| resolvePagePath(...) with undefined fallback is dead code — use tryResolvePagePath | customCheck-fixture | Slug-path hardening sprint (2026-04-21). `resolvePagePath(page)` always returns a truthy string (falls back to `/`), so `resolvePagePath(page) || undefined` is dead code that silently neutralizes downstream path-guard branches (e.g. `if (basePath) { fetch(...) }`). customCheck scans `*.ts`/`*.tsx` for the regex `/resolvePagePath\([^)]*\)\s*\|\|\s*undefined\b/`, excluding `server/helpers.ts` and `src/lib/pathUtils.ts`. Hatch is `// slug-path-ok`. All sites were refactored to `tryResolvePagePath(page)` which returns `undefined` when both slug and publishedPath are absent. 8 fixture tests in `tests/pr-check.test.ts` cover: trigger (page variable), trigger (p variable), negative (tryResolvePagePath), negative (resolvePagePath without `|| undefined`), inline hatch, above-line hatch, and canonical-implementation excludes. Rule reports ✓ on full-repo scan. |
| Manual pageMap pairing outside shared helpers — use findPageMapEntry(ForPage) or usePageJoin | customCheck-fixture | Unified page-join hooks sprint (2026-04-21). customCheck scans `src/**/*.{ts,tsx}` for `pageMap.find(` or `pageMap?.find(`, excluding `src/hooks/admin/usePageJoin.ts` (canonical hook) and `src/lib/pathUtils.ts` (canonical helpers). Hatch is `// pagemap-find-ok`. Three components (SeoEditor, PageIntelligence, ApprovalsTab) previously reimplemented inline matching with divergent semantics; all refactored to use `findPageMapEntry(ForPage)` or `usePageJoin` in the same sprint. 6 fixture tests in `tests/pr-check.test.ts` cover: `pageMap.find(` trigger, `pageMap?.find(` trigger, `usePageJoin.ts` allowlist negative, `pathUtils.ts` allowlist negative, inline hatch, and above-line hatch. Rule reports ✓ on full-repo scan. |
| useWorkspaceEvents handler for centralized event | customCheck-fixture | Broadcast-invalidation centralization sprint (2026-04-21), extended in bug sweep #598. customCheck reads `src/hooks/useWsInvalidation.ts` at runtime (fail-closed: throws if missing or has zero handler keys) to build a dynamic allowlist of centralized `WS_EVENTS.*` entries, then scans `useWorkspaceEvents(...)` handler objects in `src/**/*.{ts,tsx}` (excluding `useWsInvalidation.ts` itself and test files). It flags both computed keys (`[WS_EVENTS.ACTIVITY_NEW]`) and raw string keys (`'activity:new'`) by reverse-mapping event values from `src/lib/wsEvents.ts`. Hatch is `// ws-invalidation-ok` on the flagged line or the line immediately above, for legitimate local side effects or client-route refresh ownership. 8 fixture tests in `tests/pr-check.test.ts` cover: computed centralized-event trigger, raw string centralized-event trigger, non-centralized event negative, central-hook file negative, inline hatch, above-line hatch, raw-string hatch, and test-file negative. Rule reports ✓ on full-repo scan. |
| Route read/write contract annotations | customCheck-fixture | Bug guardrail sweep #595. customCheck enforces file-level `@reads` and `@writes` annotations near the top of the ten highest-churn route files (`keyword-strategy`, `jobs`, `webflow-seo`, `webflow-schema`, `workspaces`, `public-portal`, `content-requests`, `public-analytics`, `content-briefs`, `webflow-alt-text`) and validates annotation values are concrete comma-separated tokens or `none`, not empty/TODO/ambiguous placeholders. Hatch is `// route-contract-ok` for generated or scheduled-for-deletion route files. 10 fixture tests in `tests/pr-check.test.ts` cover: missing required tags, valid colon-form tags, malformed values, empty values, explicit `none`, misplaced tags, `@readsOnly` false-positive prevention, non-required route negative, non-required malformed-tag validation, and hatch negative. Rule reports ✓ on full-repo scan after annotating the initial enforced route set. |
| new Map from .toLowerCase() key without uniqueness proof | regex-manual | PR #267 follow-up. Pattern uses BSD-compatible POSIX classes (`[[:space:]]`, `[^]]`) because `scripts/pr-check.ts` shells out via macOS `grep -E`, which doesn't support `\s` or `[^\]]`. Verified: (a) manual `grep -rnE 'new Map\([^)]*\.map\([^)]*=>[[:space:]]*\[[^]]*\.toLowerCase\(\)' server/` returned 14 hits — the 14 known sites backfilled with `// map-dup-ok` inline (`server/competitor-snapshot-store.ts:162,163`, `server/keyword-recommendations.ts:312`, `server/content-matrices.ts:243`, 8 sites in `server/routes/keyword-strategy.ts`, `server/routes/jobs.ts:105`, `server/routes/content-briefs.ts:386`), all confirmed single-keyword-per-row sources (DataForSEO/SEMRush unique-keyword responses, DB indexes enforcing uniqueness); (b) synthetic trigger `const m = arr.map(r => [r.x.toLowerCase(), r]); new Map(m);` would match but does not exist in repo; (c) the deliberate fix at `server/analytics-intelligence.ts:1319-1324` uses `.reduce()` with a min-position merge strategy and does not match the pattern. The only way this flips from ✓ to a match is if someone reintroduces the silent-overwrite construction, which is exactly what we want to block. |
| roadmap.json item ID uniqueness | customCheck-fixture | Roadmap-redesign sprint (2026-04-22), round 4 of PR #258. customCheck reads `data/roadmap.json` directly (or a roadmap.json path passed via `files` for fixtures) and reports any `id` that appears in two or more sprints. The rule prevents the silent-failure mode where a duplicate `id` causes status toggles in the flat Backlog view to update the wrong row (React key collision + sprint-order-dependent server PATCH match). The 6 historical duplicates (291, 292, 293–296) were renumbered to 605–610 by `scripts/dedupe-roadmap-ids.ts` in the same commit; running that script is idempotent. No hatch — duplicate IDs are unconditionally a bug because `(sprintId, id)` is the only correct identity tuple. 4 fixture tests in `tests/pr-check.test.ts` cover: dup-IDs trigger, all-unique negative, malformed-JSON parse error, and cross-type (numeric vs string-form) collision. Rule reports ✓ on full-repo scan. |
| Legacy surface token in new code | regex-shell | Design-system unification Phase 1 (PR #277). Pattern `var\(--brand-bg-`; no embedded quotes, grep succeeds. The `--brand-bg-*` names are legacy aliases in `src/index.css` pointing to `--surface-N`; new code must use `--surface-N` directly. The only legitimate occurrences are in `src/index.css` itself, which is excluded from the rule. Manual `grep -rn --include="*.tsx" --include="*.css" "var(--brand-bg-" src/` returns zero hits (excluding `src/index.css`). Rule reports ✓ on full-repo scan. |
| Hand-rolled card div (use SectionCard) | customCheck-fixture | Design-system unification Phase 1/3 (PR #277). customCheck reads each `.tsx` file and regex-matches `className="[^"]*bg-zinc-9\d{2}[^"]*rounded-xl`. A 5-line lookback for `pr-check-disable-next-line` suppresses modals, tooltips, dropdowns, floating UIs, and skeleton animations that legitimately use the pattern without SectionCard semantics. All 26 pre-existing violations were either migrated to `<SectionCard>` (Phase 3, 47-file batch) or hatched with justification comments. 4 fixture tests in `tests/pr-check.test.ts` cover: trigger, SectionCard negative, inline hatch, and multi-line JSX hatch (comment 3 lines above `className=`). Rule reports ✓ on full-repo scan. |
| Hardcoded card radius outside ui primitives | regex-shell | Dashboard/admin styleguide cleanup (2026-05-02). Pattern `className="[^"]*rounded-xl`; `*.tsx` scan excludes `src/components/ui/` and `public/styleguide.html` because UI primitives and the styleguide are canonical surfaces. Manual `grep -rn --include='*.tsx' -E -e 'className="[^"]*rounded-xl' src/` returned zero non-primitive hits. `npx tsx scripts/pr-check.ts --all` reports ✓ for this rule. Synthetic trigger spot-check matched three representative hardcoded radius cases (`div`, `button`, `section`) and ignored the tokenized negative `rounded-[var(--radius-lg)]`. |
| radius-signature-lg used outside SectionCard | customCheck-fixture | Design-system unification Phase 1 (PR #277), pattern→customCheck conversion in Phase 2 Batch 1 follow-up (PR #305). customCheck scans `*.tsx`/`*.css` for `--radius-signature-lg`, excluding `SectionCard.tsx`, `src/tokens.css`, `public/tokens.css`, `public/styleguide.html`, `public/styleguide.css`. SectionCard.tsx owns the canonical asymmetric `10px 24px 10px 24px` chrome. Non-SectionCard containers MAY use the token directly when the asymmetric brand signature is intentional — they must declare per-site intent with `// pr-check-disable-next-line -- <reason>` (5-line lookback to support multi-attribute JSX). 4 fixture tests in `tests/pr-check.test.ts` cover: trigger without hatch, inline hatch, hatch on the line above, and 5-line-lookback hatch. Rule reports ✓ on full-repo scan. |
| Non-standard transition duration | customCheck-fixture | Design-system unification Phase 1 (PR #277). customCheck scans `*.tsx`/`*.css` (excluding `src/components/ui/`, styleguide files) for `transition-duration-[Xms]` Tailwind utilities, flagging any duration not in `{120ms, 180ms, 400ms}`. The three-speed motion system is: 120ms (micro-interaction), 180ms (standard UI), 400ms (entrance/exit). All existing usages in the codebase use `transition-colors`/`transition-all` without explicit duration utilities, so no backfill was needed. 4 fixture tests in `tests/pr-check.test.ts` cover: non-standard duration trigger, all-three-allowed negatives, multiple-hits trigger, and no-transition-duration-utility negative. Rule reports ✓ on full-repo scan. |
| Hand-rolled trend badge | regex-shell + fixture | Design-system unification Phase 4 (PR #TBD). Pattern `<TrendingUp[^>]*/>[^<]*:[^<]*<TrendingDown\|<TrendingDown[^>]*/>[^<]*:[^<]*<TrendingUp` in `*.tsx`, pathFilter `src/components/`, excluding `src/components/ui/TrendBadge.tsx` (the primitive itself). Hatch is `// trend-badge-ok` on the match line. All 8 historical callsites (DataSnapshots ×5 ChangeBadge, RevenueDashboard:134-137, AuditHistory:47-49, SchemaSuggester:979, SeoChangeImpact:65-68) were migrated to `<TrendBadge>` from `src/components/ui/TrendBadge.tsx` in the same PR. `ChatBlocks.tsx:34` uses a 3-way ternary with `<Minus>` that does not match the pattern (`[^<]*` breaks on the `m.change < 0` comparator) — acceptable miss; the regex targets the canonical 2-arm up/down pattern. 5 fixture tests in `tests/pr-check.test.ts` cover: trigger (standard order), trigger (reversed Down/Up), proper `<TrendBadge>` negative, TrendBadge.tsx self-exclusion, and inline hatch. Rule reports ✓ on full-repo scan. Ships as `warn`; promote to `error` once the long tail is fully migrated. |
| Raw hex chart color in component (use CHART_SERIES_COLORS) | backfill-complete | Design-system Phase 6C. Pattern `(stroke\|fill\|color)=["']#[0-9a-fA-F]{6}["']` in `*.tsx`, pathFilter `src/components/`, excluding `constants.ts`. All hardcoded chart hex references were migrated to `CHART_SERIES_COLORS` imports from `ui/constants.ts`, or hatched with `// chart-hex-ok` for special-use colors (cyan-400, bespoke teal, provider brand colors). `excludeLines` also exempts `chartDotStroke`, `chartDotFill`, `chartGridColor`, `scoreColor(`, and `url(#` (gradient refs). Rule reports ✓ on full-repo scan after migration. Ships as `warn`. |
| SectionCard titleExtra with ml-auto (use action prop) | customCheck-fixture | Design-system unification Phase 3 (PR #277). customCheck walks each `titleExtra={...}` prop via paren-depth balancing (up to 15 lines forward from the `titleExtra=` opener) and flags any occurrence of `ml-auto` inside the brace range. SectionCard's `titleExtra` slot renders inside the LEFT-aligned title cluster which has no `flex-grow`, so `ml-auto` on a child has no effect — this regex catches the recurring "right-aligned metadata silently lands on the left" bug that hit 5 call sites during Phase 3 migration (OrderStatus, RankTable, DataSnapshots, SearchTab, FixRecommendations). Hatch is `pr-check-disable-next-line` on the `titleExtra=` line or the line immediately above. Excludes `src/components/ui/` (SectionCard itself). 5 fixture tests in `tests/pr-check.test.ts` cover: single-line trigger, multi-line trigger with `ml-auto` on a nested line, valid `titleExtra` badge negative (no ml-auto), correct `action={...ml-auto...}` negative, and inline hatch. Rule reports ✓ on full-repo scan. |
| styleguide-typography-parity | customCheck-fixture | Phase 6A. customCheck compares .t-* class definitions in `public/styleguide.css` against canonical values in `src/index.css` (font-size, font-weight, line-height, letter-spacing). Uses CSS value normalization (-.03em = -0.03em) to avoid false positives. 3 fixture tests in `tests/pr-check.test.ts` cover: current-repo-state pass (both files in sync), rule has customCheck, and unrelated-file skip. Rule reports ✓ after Phase 6A sync of all 14 .t-* classes. |
| styleguide-token-parity | customCheck-fixture | Phase 5 Task 0.2. customCheck scans files matching the `public/styleguide.css` path suffix for `--*` token declarations. All tokens must come via `@import url('/tokens.css')` — re-declarations create drift between the styleguide and app. 4 fixture tests in `tests/pr-check.test.ts` cover: trigger (`--brand-mint` re-declared in styleguide.css), clean negative (no `--*` declarations), non-styleguide file negative (other.css not matched), and non-existent file negative (no throw). Rule reports ✓ since `public/styleguide.css` does not yet exist in Phase 0. |
| Comma in arbitrary grid-cols/grid-rows (Tailwind v4) | regex-shell + fixture | Post-Phase-2.1a smoke-test discovery. Tailwind v4 emits the substring inside `[...]` verbatim, swapping `_` for space; commas are NOT swapped. So `grid-cols-[1fr,80px,80px]` emits invalid `grid-template-columns: 1fr,80px,80px` and the browser silently collapses to a single column (items stack vertically). Pattern `grid-(cols\|rows)-\[[^(]*,` — the `[^(]*` lookahead allows commas inside CSS functions like `minmax(100px,1fr)` or `repeat(3,1fr)` while still catching top-level track-list commas. The 4 pre-existing violations (`src/components/RankTracker.tsx` ×2, `src/components/client/FixRecommendations.tsx` ×2, both `grid-cols-[1fr,...]`) were fixed to underscore syntax in the same PR. 5 fixture tests in `tests/pr-check.test.ts` cover: comma grid-cols trigger, comma grid-rows trigger, underscore grid-cols negative, `minmax(100px,1fr)` function-internal comma negative, and `repeat(3,1fr)` function-internal comma negative. Rule reports ✓ on full-repo scan. |
| SectionCard className double-wrap trap (space-y-* always wrong; p[xy]?-* needs noPadding) | customCheck-fixture | Phase 2 Batch 2 Devin review pattern (PR #307). SectionCard wraps children in `<div className={noPadding ? '' : 'p-4'}>`, so the outer div ALWAYS has exactly one direct child (the inner wrapper). Two consequences: (a) `space-y-N`/`space-x-N` on outer NEVER reaches children — fix by wrapping children in a spaced div; (b) `p[xy]?-N` on outer compounds with the inner `p-4` doubling padding — fix with the `noPadding` prop. customCheck walks `<SectionCard` openings (skipping identifier-prefixed forms like `SectionCardProps`), brace-balances forward up to 15 lines to capture the full opening tag, then runs `\bspace-[xy]-\d` and `\bp[xy]?-(?:\d\|\[)` regexes against the extracted className value (handles both `className="..."` and `className={...}` forms). Hatch is `// sectioncard-className-ok` on the SectionCard line or the line immediately above for the rare legitimate override (e.g. an offset margin). Excludes `src/components/ui/`. The 6 violations Devin flagged in PR #307 (`SeoAudit.tsx:691`, `SeoAuditGuide.tsx:129/158/187`, `RedirectManager.tsx:195/487`) were fixed in the same PR. 10 fixture tests cover: space-y trigger, space-x trigger, px-* without noPadding trigger, px-* WITH noPadding negative, clean className negative, no className negative, inline hatch, above-line hatch, multi-line SectionCard with className on a later line, and identifier-prefixed `SectionCardProps` negative. Rule reports ✓ on full-repo scan. |
| Forbidden hues (rose/pink) in components | regex-shell + fixture | Phase 5 Phase 3 enforcement hardening (2026-04-25). Pattern `\b(rose\|pink)-[0-9]` with word-boundary anchor to avoid `prose-` false positives (Tailwind typography plugin). Verified zero hits across `src/`. Four Laws Law extension: rose and pink are forbidden alongside violet and indigo; allowed hues are teal/blue/emerald/amber/red. 4 fixture tests cover: rose-{N} trigger, pink-{N} trigger, prose- substring negative, and allowed-hue negative. |
| text-green-{N} for success/score (use emerald) | regex-shell + fixture | Phase 5 Phase 3 enforcement hardening (2026-04-25). Pattern `text-green-[0-9]`. CLAUDE.md's Four Laws Law #3 explicitly bans `text-green-400` for success/score indicators in favor of emerald — this rule mechanizes the warning that previously lived only in prose. Hatch is `// green-ok` for the rare non-success use (brand chart series, third-party). Verified zero hits across `src/` after migrating `RequestList.tsx:235` Deliver Brief button from green-* to emerald-* in the same PR. 4 fixture tests cover: text-green-400 trigger, text-green-500 trigger, text-emerald-400 negative, and `// green-ok` hatch suppression. |
| No purple/violet in client domain | regex-shell | Phase C new rule (2026-04-27). Pattern `(purple\|violet)-[0-9]+`; pathFilter `src/components/client/`. Stricter than the existing "Purple in client components" rule — also catches `violet-`. Manual `grep -rn --include='*.tsx' --include='*.ts' -E '(purple\|violet)-[0-9]+' src/components/client/` returns zero hits. Mechanizes Law 04 of the Four Laws of Color. 3 fixture tests cover: purple-400 trigger, violet-600 trigger, and teal-400 negative. |
| score-color-law-parity | customCheck-fixture | Phase C new rule (2026-04-27). customCheck parses `src/components/ui/constants.ts`, extracts the `scoreColorClass` function body, and flags any `green-\d+` references (should use `emerald-*` per Law 03). Verified the current `scoreColorClass` returns `text-emerald-400` / `text-amber-400` / `text-red-400` — zero hits. 3 fixture tests cover: `text-green-400` trigger, correct `text-emerald-400` negative, and missing-function negative. |
| Raw z-index class (use z-[var(--z-*)] tokens) | regex-shell | Phase 6B z-index tokenization (2026-04-28). Pattern `\bz-(10|20|30|40|50|60)\b` in `src/`. Migrated ~62 raw z-index Tailwind classes across 39 files to token form `z-[var(--z-*)]`. Non-standard values (z-[70], z-[80], z-[100], z-[200], z-[9999]) have `// z-index-ok` escape hatches. Tokens defined in `src/tokens.css:114-120`. |
| HTML-naive word count on rich-text post field | regex-shell + fixture | PR #356 blog-editor migration (2026-04-28). Pattern `(countWords\\([^()]*\\.(introduction\|conclusion)\|(introduction\|conclusion)\\.split\\()` — POSIX-compatible (no `\b`/`\w`/`\s`) so BSD `grep -E` on macOS works. Catches `countWords(post.introduction)` (HTML treated as plain text → tags counted as words) and bare `post.introduction.split(/\s+/)` patterns. The `[^()]*` (vs `[^)]*`) is critical: it excludes `countWords(stripHtml(post.introduction))` and `countWords(stripHtml(post.conclusion))` because the inner `(` from `stripHtml(` breaks the no-parens match. Same root cause as the section.wordCount drift Devin flagged in PR #356 (8 sites fixed: `PostEditor.tsx`, `public-content.ts`, `content-posts.ts`, `content-posts-ai.ts`). Hatch `// html-word-count-ok`. 8 fixture tests cover: 4 trigger cases (countWords on introduction/conclusion, .split on introduction/conclusion), 3 negative cases (countHtmlWords helper, countWords(stripHtml(...)), countWords on plain-text title), and 1 hatch case. |
| schema-context-direct-read-not-on-allowlist | customCheck-fixture | Schema Yoast parity PR1 Task 13 (2026-04-29), hardened during SEO context retirement completion (2026-05-07). customCheck brace-walks `buildSchemaContext` in `server/helpers.ts`. Uses a two-phase signature scanner: paren+angle depth tracking to find the real body-opening `{` (past the multi-line `Promise<{ ... } & SchemaAnalyticsMaps>` return type), then brace-depth counting to find the function close. Identity fields (`ws.name`, `ws.id`, `ws.liveDomain`, `ws.brandLogoUrl`, `ws.siteHasSearch`, `siteId`) are allow-listed. For non-identity reads, `// schema-context-direct-read-ok: <reason>` still works except for migrated-forbidden fields (`ws.brandVoice`, `ws.keywordStrategy`, `ws.businessProfile`), which are now always errors even with hatch comments. 8 fixture tests in `tests/pr-check.test.ts` cover: trigger (ws.gscPropertyUrl with no hatch), allowlist negative, inline hatch, above-line hatch, wrong-file negative, and three migrated-forbidden hatch-bypass triggers (brandVoice, keywordStrategy, businessProfile). Rule reports ✓ on full-repo scan. |
| schema.org property key foundedDate (should be foundingDate) | regex-shell | PR #406 schema semantic extraction (2026-05-01). Pattern `'foundedDate'[[:space:]]*:` in `server/schema/templates/`. Catches templates that emit `foundedDate` in JSON-LD output — schema.org's canonical property is `foundingDate`; Google silently ignores unrecognised properties. Both `homepage.ts` and `local-business.ts` were fixed in this PR (renamed to `foundingDate`). The corresponding unit test assertion in `tests/unit/schema/templates.test.ts:599` was updated in the same commit. Manual `grep -rn --include='*.ts' "'foundedDate'" server/schema/templates/` returns zero hits. Hatch `// schema-property-name-ok`. |
| Unfiltered staff image URL in schema template (use filterHttpUrls) | regex-shell | PR #406 schema semantic extraction (2026-05-01). Pattern `'image'[[:space:]]*:[[:space:]]*s\.image` in `server/schema/templates/`. Staff `Person` nodes are populated from AI-extracted semantics where `s.image` may be an attacker-controllable URL (data: URI, javascript: scheme, etc.) from page HTML. All three templates that emit staff nodes (`local-business.ts`, `service.ts`, `static.ts`) were fixed to use `filterHttpUrls([s.image ?? ''])[0]`. Manual `grep -rn --include='*.ts' -E "'image'[[:space:]]*:[[:space:]]*s\.image" server/schema/templates/` returns zero hits. Hatch `// staff-image-filter-ok`. |
| Unfiltered semantics.primaryImage in schema template (use filterHttpUrls) | regex-shell | PR #406 schema semantic extraction (2026-05-01). Pattern `'image'[[:space:]]*:[[:space:]]*semantics?\??.primaryImage` in `server/schema/templates/`. `semantics.primaryImage` is AI-extracted from attacker-controllable page HTML, same risk as `s.image`. `homepage.ts` and `service.ts` were originally missing the filter that `article.ts` and `local-business.ts` had — fixed in the same PR. All four templates now use `filterHttpUrls([semantics?.primaryImage ?? '', pageData.image ?? ''])[0]`. Manual grep returns zero hits. Hatch `// primary-image-filter-ok`. |
| inbox-legacy-filter-literal | customCheck-fixture | 2026-05-08 inbox redesign. customCheck scans `src/**/*.{ts,tsx}` for `[?&]tab=(approvals\|requests\|content-plan\|copy)` as URL query parameters. Implemented as customCheck (not pattern-based grep) so comment-only lines, JSX block comments (`{/* */}`), and C-style block comments (`/* */`) are skipped without needing per-file escape hatches. Hatch is `// inbox-legacy-filter-literal-ok` on the flagged line or the immediately preceding line. Pre-checked: `grep -rn "tab=approvals\|tab=requests\|tab=content-plan\|tab=copy" src/ \| grep -v "// inbox-legacy-filter-literal-ok"` returns 2 comment-only lines in `App.tsx` (correctly skipped by the customCheck); zero non-comment hits. 9 fixture tests in `tests/pr-check.test.ts` cover: `?tab=approvals` trigger, `?tab=requests` trigger, `&tab=copy` trigger, `?tab=content-plan` trigger, `?tab=seo-changes` negative, `?tab=needs-action` negative, comment-only line negative, inline hatch, and above-line hatch. Rule reports ✓ on full-repo scan. |
| feedback-module-reintroduction | customCheck-fixture | PR 1.1 (2026-05-08) retired the FeedbackWidget module and /api/feedback route. customCheck scans all `*.ts` and `*.tsx` files for `import[^'"\`]*['"\`][^'"\`]*FeedbackWidget[^'"\`]*['"\`]` (import) and `router\.(get|post|put|patch|delete)\s*\(\s*['"\`][/]api[/](public[/])?feedback` (route registration), skipping pure `//` comment lines. Hatch `feedback-module-reintroduction-ok`. Manual `grep -rn "FeedbackWidget\|/api/feedback" src/ server/` returns zero non-comment hits. Fixture tests (Task 5) cover: import trigger, comment skip, hatch suppression. Rule reports ✓ on full-repo scan. |
| keyword-strategy-action-type | regex-shell | Pattern `(sourceType\|type)\s*:\s*['"]keyword_strategy['"]`; pathFilter `src/`; excludes `shared/types/` and `server/db/migrations/` (type definition and migration files legitimately reference the retired literal). Hatch `keyword-strategy-action-type-ok`. Manual `grep -rn --include='*.ts' --include='*.tsx' -E "(sourceType\|type)\s*:\s*['\"]keyword_strategy['\"]" src/` returns zero hits. Rule reports ✓ on full-repo scan. |
| send-for-review-anti-pattern | backfill-complete | Pattern `Send for Review\|Flag for Client`; pathFilter `src/`; `*.tsx` files only. Pre-existing violations in `src/components/matrix/CellDetailPanel.tsx:274` and `src/components/matrix/MatrixGrid.tsx:259` suppressed with `// send-for-review-anti-pattern-ok` hatch (matrix component predates Admin Send Convention; will be migrated separately). Hatch `send-for-review-anti-pattern-ok`. Rule reports ✓ on full-repo scan after hatch comments added. |
| prediction-showcase-ungated | customCheck-fixture | customCheck scans `src/**/*.tsx` for `<PredictionShowcaseCard` on non-comment lines and checks whether `!winsEnabled` appears on the same line or within 2 preceding lines. Pre-existing usage in `src/components/client/OverviewTab.tsx:331` suppressed with `{/* prediction-showcase-ungated-ok */}` on the immediately preceding line. Hatch `prediction-showcase-ungated-ok`. Fixture tests (Task 5) cover: bare usage trigger, guarded usage negative, hatch suppression. Rule reports ✓ on full-repo scan. |
| inbox-action-queue-strip | backfill-complete | Pattern `ActionQueueStrip`; restricted to files named exactly `InboxTab.tsx`. Pre-existing JSDoc comment reference in `src/components/client/InboxTab.tsx:36` suppressed with `inbox-action-queue-strip-ok` appended to the comment line (pattern-based grep matches the comment; hatch is placed inline on the matched line). Hatch `inbox-action-queue-strip-ok`. Rule reports ✓ on full-repo scan after hatch comment added. |

**Count: 98 verified-clean rules.**

> Note: `Hand-rolled gradient CTA button` (added in Phase 5 Phase 3) ships at
> warn severity because a full-repo `--all` scan finds ~11 remaining
> hand-rolled gradient CTAs across older surfaces. Promote to error in a
> follow-up PR after the dedicated migration sweep clears the backlog.

---

## What "verified by" means

- **regex-shell** — the rule's regex pattern has been manually invoked via
  `grep -rn ... -E "${pattern}" src/` (or the relevant root) and confirmed
  to produce either the expected matches or a clean zero with no shell
  error. This catches Categories B and D (regex too narrow, shell
  quoting).
- **regex-manual** — the regex was run manually in `node -e` or similar
  to confirm it matches at least one synthetic trigger and rejects at
  least one synthetic negative.
- **customCheck-fixture** — the rule has a `describe('Rule: <name>', ...)`
  block in `tests/pr-check.test.ts` with at least trigger, negative, and
  hatch tests. This is the strongest guarantee — a regression in the
  customCheck callback will be caught by the harness, not by silent
  passage through `pr-check --all`.
- **backfill-complete** — the rule was previously `⚠` with real
  violations, the violations have been fixed, and re-running
  `pr-check --all` shows `✓`. Prefer this over manual spot-checks.

When a rule's verification method changes (e.g., a `regex-shell` rule is
converted to `customCheck`), update the row in the same commit as the
refactor.

---

## Removing a rule

If a rule should no longer report `✓` (e.g., a new backfill item surfaces
violations, or the rule is deleted), remove its row from the table
**before** the commit that causes the state change. The meta-test will
otherwise fail with a mismatch error that points at this file.
